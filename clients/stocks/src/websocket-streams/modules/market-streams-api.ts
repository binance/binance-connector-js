/**
 * Stocks Trading WebSocket Streams
 *
 * WebSocket stream definitions for Binance Stocks Trading. Base URL: wss://nbstream.binance.com/equity
 *
 * The version of the OpenAPI document: 1.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import {
    WebsocketStreamsBase,
    WebsocketStream,
    assertParamExists,
    replaceWebsocketStreamsPlaceholders,
    createStreamHandler,
} from '@binance/common';
import type {
    CalendarStreamResponse,
    KlineStreamResponse,
    PriceStreamResponse,
    QuoteStreamResponse,
    TradabilityStreamResponse,
    TradingStatusStreamResponse,
} from '../types';

const MarketStreamsApiParamCreator = function () {
    return {
        /**
         * Single-stream broadcast of market-phase transitions. One message per transition; no periodic heartbeat payload. Server polls every 5 seconds. Also reachable via the SUBSCRIBE/UNSUBSCRIBE RPC — see [Subscribing via RPC](https://developers.binance.com/en/docs/products/stocks/websocket-streams-general-info#subscribing-via-rpc).
         *
         * @summary Calendar Stream
         * @param {string} [id] Unique WebSocket request ID.
         *
         * @throws {RequiredError}
         */
        calendarStream: (id?: string): string => {
            return replaceWebsocketStreamsPlaceholders('/calendar'.slice(1), { id });
        },
        /**
         * Per-symbol candlestick updates. One stream per (symbol, interval) combination. Supported intervals: 5m, 1h, 1d, 1w, 1M. Also reachable via the SUBSCRIBE/UNSUBSCRIBE RPC — see [Subscribing via RPC](https://developers.binance.com/en/docs/products/stocks/websocket-streams-general-info#subscribing-via-rpc).
         *
         * @summary Kline Stream
         * @param {string} symbol US-equity ticker (UPPERCASE), e.g. `AAPL`.
         * @param {string} interval Kline interval — `5m` / `1h` / `1d` / `1w` / `1M`.
         * @param {string} [id] Unique WebSocket request ID.
         *
         * @throws {RequiredError}
         */
        klineStream: (symbol: string, interval: string, id?: string): string => {
            // verify required parameter 'symbol' is not null or undefined
            assertParamExists('klineStream', 'symbol', symbol);
            // verify required parameter 'interval' is not null or undefined
            assertParamExists('klineStream', 'interval', interval);

            return replaceWebsocketStreamsPlaceholders('/<symbol>@kline_<interval>'.slice(1), {
                symbol,
                interval,
                id,
            });
        },
        /**
         * Push-all price snapshot, polled every 3 seconds. One message carries the latest price for every active US-equity symbol. Also reachable via the SUBSCRIBE/UNSUBSCRIBE RPC — see [Subscribing via RPC](https://developers.binance.com/en/docs/products/stocks/websocket-streams-general-info#subscribing-via-rpc).
         *
         * @summary Price Stream
         * @param {string} [id] Unique WebSocket request ID.
         *
         * @throws {RequiredError}
         */
        priceStream: (id?: string): string => {
            return replaceWebsocketStreamsPlaceholders('/price'.slice(1), { id });
        },
        /**
         * Per-symbol real-time best-bid / best-ask. Each symbol has its own stream. Per-symbol throttle: at most one push per symbol every 200 ms. Also reachable via the SUBSCRIBE/UNSUBSCRIBE RPC — see [Subscribing via RPC](https://developers.binance.com/en/docs/products/stocks/websocket-streams-general-info#subscribing-via-rpc).
         *
         * @summary Quote Stream
         * @param {string} symbol US-equity ticker (UPPERCASE), e.g. `AAPL`.
         * @param {string} [id] Unique WebSocket request ID.
         *
         * @throws {RequiredError}
         */
        quoteStream: (symbol: string, id?: string): string => {
            // verify required parameter 'symbol' is not null or undefined
            assertParamExists('quoteStream', 'symbol', symbol);

            return replaceWebsocketStreamsPlaceholders('/<symbol>@quote'.slice(1), { symbol, id });
        },
        /**
         * Per-symbol push whenever the tradable direction of a symbol changes. Pushed only when the value actually changes (new value ≠ old value). Also reachable via the SUBSCRIBE/UNSUBSCRIBE RPC — see [Subscribing via RPC](https://developers.binance.com/en/docs/products/stocks/websocket-streams-general-info#subscribing-via-rpc).
         *
         * @summary Tradability Stream
         * @param {string} symbol US-equity ticker (UPPERCASE), e.g. `AAPL`.
         * @param {string} [id] Unique WebSocket request ID.
         *
         * @throws {RequiredError}
         */
        tradabilityStream: (symbol: string, id?: string): string => {
            // verify required parameter 'symbol' is not null or undefined
            assertParamExists('tradabilityStream', 'symbol', symbol);

            return replaceWebsocketStreamsPlaceholders('/<symbol>@tradability'.slice(1), {
                symbol,
                id,
            });
        },
        /**
         * Per-symbol trading-status transitions (halts, resumes, SSR, LULD pauses, etc.). Events that do not match a known status/reason rule are not pushed. Also reachable via the SUBSCRIBE/UNSUBSCRIBE RPC — see [Subscribing via RPC](https://developers.binance.com/en/docs/products/stocks/websocket-streams-general-info#subscribing-via-rpc).
         *
         * @summary Trading Status Stream
         * @param {string} symbol US-equity ticker (UPPERCASE), e.g. `AAPL`.
         * @param {string} [id] Unique WebSocket request ID.
         *
         * @throws {RequiredError}
         */
        tradingStatusStream: (symbol: string, id?: string): string => {
            // verify required parameter 'symbol' is not null or undefined
            assertParamExists('tradingStatusStream', 'symbol', symbol);

            return replaceWebsocketStreamsPlaceholders('/<symbol>@tradingStatus'.slice(1), {
                symbol,
                id,
            });
        },
    };
};

/**
 * MarketStreamsApi - interface
 * @interface MarketStreamsApi
 */
export interface MarketStreamsApiInterface {
    /**
     * Single-stream broadcast of market-phase transitions. One message per transition; no periodic heartbeat payload. Server polls every 5 seconds. Also reachable via the SUBSCRIBE/UNSUBSCRIBE RPC — see [Subscribing via RPC](https://developers.binance.com/en/docs/products/stocks/websocket-streams-general-info#subscribing-via-rpc).
     *
     * @summary Calendar Stream
     * @param {CalendarStreamRequest} requestParameters Request parameters.
     *
     * @returns {WebsocketStream<CalendarStreamResponse>}
     * @throws {RequiredError}
     * @memberof MarketStreamsApiInterface
     */
    calendarStream(
        requestParameters?: CalendarStreamRequest
    ): WebsocketStream<CalendarStreamResponse>;

    /**
     * Per-symbol candlestick updates. One stream per (symbol, interval) combination. Supported intervals: 5m, 1h, 1d, 1w, 1M. Also reachable via the SUBSCRIBE/UNSUBSCRIBE RPC — see [Subscribing via RPC](https://developers.binance.com/en/docs/products/stocks/websocket-streams-general-info#subscribing-via-rpc).
     *
     * @summary Kline Stream
     * @param {KlineStreamRequest} requestParameters Request parameters.
     *
     * @returns {WebsocketStream<KlineStreamResponse>}
     * @throws {RequiredError}
     * @memberof MarketStreamsApiInterface
     */
    klineStream(requestParameters: KlineStreamRequest): WebsocketStream<KlineStreamResponse>;

    /**
     * Push-all price snapshot, polled every 3 seconds. One message carries the latest price for every active US-equity symbol. Also reachable via the SUBSCRIBE/UNSUBSCRIBE RPC — see [Subscribing via RPC](https://developers.binance.com/en/docs/products/stocks/websocket-streams-general-info#subscribing-via-rpc).
     *
     * @summary Price Stream
     * @param {PriceStreamRequest} requestParameters Request parameters.
     *
     * @returns {WebsocketStream<PriceStreamResponse>}
     * @throws {RequiredError}
     * @memberof MarketStreamsApiInterface
     */
    priceStream(requestParameters?: PriceStreamRequest): WebsocketStream<PriceStreamResponse>;

    /**
     * Per-symbol real-time best-bid / best-ask. Each symbol has its own stream. Per-symbol throttle: at most one push per symbol every 200 ms. Also reachable via the SUBSCRIBE/UNSUBSCRIBE RPC — see [Subscribing via RPC](https://developers.binance.com/en/docs/products/stocks/websocket-streams-general-info#subscribing-via-rpc).
     *
     * @summary Quote Stream
     * @param {QuoteStreamRequest} requestParameters Request parameters.
     *
     * @returns {WebsocketStream<QuoteStreamResponse>}
     * @throws {RequiredError}
     * @memberof MarketStreamsApiInterface
     */
    quoteStream(requestParameters: QuoteStreamRequest): WebsocketStream<QuoteStreamResponse>;

    /**
     * Per-symbol push whenever the tradable direction of a symbol changes. Pushed only when the value actually changes (new value ≠ old value). Also reachable via the SUBSCRIBE/UNSUBSCRIBE RPC — see [Subscribing via RPC](https://developers.binance.com/en/docs/products/stocks/websocket-streams-general-info#subscribing-via-rpc).
     *
     * @summary Tradability Stream
     * @param {TradabilityStreamRequest} requestParameters Request parameters.
     *
     * @returns {WebsocketStream<TradabilityStreamResponse>}
     * @throws {RequiredError}
     * @memberof MarketStreamsApiInterface
     */
    tradabilityStream(
        requestParameters: TradabilityStreamRequest
    ): WebsocketStream<TradabilityStreamResponse>;

    /**
     * Per-symbol trading-status transitions (halts, resumes, SSR, LULD pauses, etc.). Events that do not match a known status/reason rule are not pushed. Also reachable via the SUBSCRIBE/UNSUBSCRIBE RPC — see [Subscribing via RPC](https://developers.binance.com/en/docs/products/stocks/websocket-streams-general-info#subscribing-via-rpc).
     *
     * @summary Trading Status Stream
     * @param {TradingStatusStreamRequest} requestParameters Request parameters.
     *
     * @returns {WebsocketStream<TradingStatusStreamResponse>}
     * @throws {RequiredError}
     * @memberof MarketStreamsApiInterface
     */
    tradingStatusStream(
        requestParameters: TradingStatusStreamRequest
    ): WebsocketStream<TradingStatusStreamResponse>;
}

/**
 * Request parameters for calendarStream operation in MarketStreamsApi.
 * @interface CalendarStreamRequest
 */
export interface CalendarStreamRequest {
    /**
     * Unique WebSocket request ID.
     * @type {string}
     * @memberof MarketStreamsApiCalendarStream
     */
    readonly id?: string;
}

/**
 * Request parameters for klineStream operation in MarketStreamsApi.
 * @interface KlineStreamRequest
 */
export interface KlineStreamRequest {
    /**
     * US-equity ticker (UPPERCASE), e.g. `AAPL`.
     * @type {string}
     * @memberof MarketStreamsApiKlineStream
     */
    readonly symbol: string;

    /**
     * Kline interval — `5m` / `1h` / `1d` / `1w` / `1M`.
     * @type {string}
     * @memberof MarketStreamsApiKlineStream
     */
    readonly interval: string;

    /**
     * Unique WebSocket request ID.
     * @type {string}
     * @memberof MarketStreamsApiKlineStream
     */
    readonly id?: string;
}

/**
 * Request parameters for priceStream operation in MarketStreamsApi.
 * @interface PriceStreamRequest
 */
export interface PriceStreamRequest {
    /**
     * Unique WebSocket request ID.
     * @type {string}
     * @memberof MarketStreamsApiPriceStream
     */
    readonly id?: string;
}

/**
 * Request parameters for quoteStream operation in MarketStreamsApi.
 * @interface QuoteStreamRequest
 */
export interface QuoteStreamRequest {
    /**
     * US-equity ticker (UPPERCASE), e.g. `AAPL`.
     * @type {string}
     * @memberof MarketStreamsApiQuoteStream
     */
    readonly symbol: string;

    /**
     * Unique WebSocket request ID.
     * @type {string}
     * @memberof MarketStreamsApiQuoteStream
     */
    readonly id?: string;
}

/**
 * Request parameters for tradabilityStream operation in MarketStreamsApi.
 * @interface TradabilityStreamRequest
 */
export interface TradabilityStreamRequest {
    /**
     * US-equity ticker (UPPERCASE), e.g. `AAPL`.
     * @type {string}
     * @memberof MarketStreamsApiTradabilityStream
     */
    readonly symbol: string;

    /**
     * Unique WebSocket request ID.
     * @type {string}
     * @memberof MarketStreamsApiTradabilityStream
     */
    readonly id?: string;
}

/**
 * Request parameters for tradingStatusStream operation in MarketStreamsApi.
 * @interface TradingStatusStreamRequest
 */
export interface TradingStatusStreamRequest {
    /**
     * US-equity ticker (UPPERCASE), e.g. `AAPL`.
     * @type {string}
     * @memberof MarketStreamsApiTradingStatusStream
     */
    readonly symbol: string;

    /**
     * Unique WebSocket request ID.
     * @type {string}
     * @memberof MarketStreamsApiTradingStatusStream
     */
    readonly id?: string;
}

/**
 * MarketStreamsApi - interface
 * @class MarketStreamsApi
 * @extends {WebsocketStreamsBase}
 */
export class MarketStreamsApi implements MarketStreamsApiInterface {
    private readonly websocketBase: WebsocketStreamsBase;
    private localVarParamCreator;

    constructor(websocketBase: WebsocketStreamsBase) {
        this.websocketBase = websocketBase;
        this.localVarParamCreator = MarketStreamsApiParamCreator();
    }

    /**
     * Single-stream broadcast of market-phase transitions. One message per transition; no periodic heartbeat payload. Server polls every 5 seconds. Also reachable via the SUBSCRIBE/UNSUBSCRIBE RPC — see [Subscribing via RPC](https://developers.binance.com/en/docs/products/stocks/websocket-streams-general-info#subscribing-via-rpc).
     *
     * @summary Calendar Stream
     * @param {CalendarStreamRequest} requestParameters Request parameters.
     * @returns {WebsocketStream<CalendarStreamResponse>}
     * @throws {RequiredError}
     * @memberof MarketStreamsApi
     * @see {@link https://developers.binance.com/en/docs/catalog/advanced-trading-stocks-trading/api/ws-streams/market-streams#calendar-stream Binance API Documentation}
     */
    public calendarStream(
        requestParameters: CalendarStreamRequest = {}
    ): WebsocketStream<CalendarStreamResponse> {
        const stream = this.localVarParamCreator.calendarStream(requestParameters?.id);

        return createStreamHandler<CalendarStreamResponse>(
            this.websocketBase,
            stream,
            requestParameters?.id
        );
    }

    /**
     * Per-symbol candlestick updates. One stream per (symbol, interval) combination. Supported intervals: 5m, 1h, 1d, 1w, 1M. Also reachable via the SUBSCRIBE/UNSUBSCRIBE RPC — see [Subscribing via RPC](https://developers.binance.com/en/docs/products/stocks/websocket-streams-general-info#subscribing-via-rpc).
     *
     * @summary Kline Stream
     * @param {KlineStreamRequest} requestParameters Request parameters.
     * @returns {WebsocketStream<KlineStreamResponse>}
     * @throws {RequiredError}
     * @memberof MarketStreamsApi
     * @see {@link https://developers.binance.com/en/docs/catalog/advanced-trading-stocks-trading/api/ws-streams/market-streams#kline-stream Binance API Documentation}
     */
    public klineStream(
        requestParameters: KlineStreamRequest
    ): WebsocketStream<KlineStreamResponse> {
        const stream = this.localVarParamCreator.klineStream(
            requestParameters?.symbol,
            requestParameters?.interval,
            requestParameters?.id
        );

        return createStreamHandler<KlineStreamResponse>(
            this.websocketBase,
            stream,
            requestParameters?.id
        );
    }

    /**
     * Push-all price snapshot, polled every 3 seconds. One message carries the latest price for every active US-equity symbol. Also reachable via the SUBSCRIBE/UNSUBSCRIBE RPC — see [Subscribing via RPC](https://developers.binance.com/en/docs/products/stocks/websocket-streams-general-info#subscribing-via-rpc).
     *
     * @summary Price Stream
     * @param {PriceStreamRequest} requestParameters Request parameters.
     * @returns {WebsocketStream<PriceStreamResponse>}
     * @throws {RequiredError}
     * @memberof MarketStreamsApi
     * @see {@link https://developers.binance.com/en/docs/catalog/advanced-trading-stocks-trading/api/ws-streams/market-streams#price-stream Binance API Documentation}
     */
    public priceStream(
        requestParameters: PriceStreamRequest = {}
    ): WebsocketStream<PriceStreamResponse> {
        const stream = this.localVarParamCreator.priceStream(requestParameters?.id);

        return createStreamHandler<PriceStreamResponse>(
            this.websocketBase,
            stream,
            requestParameters?.id
        );
    }

    /**
     * Per-symbol real-time best-bid / best-ask. Each symbol has its own stream. Per-symbol throttle: at most one push per symbol every 200 ms. Also reachable via the SUBSCRIBE/UNSUBSCRIBE RPC — see [Subscribing via RPC](https://developers.binance.com/en/docs/products/stocks/websocket-streams-general-info#subscribing-via-rpc).
     *
     * @summary Quote Stream
     * @param {QuoteStreamRequest} requestParameters Request parameters.
     * @returns {WebsocketStream<QuoteStreamResponse>}
     * @throws {RequiredError}
     * @memberof MarketStreamsApi
     * @see {@link https://developers.binance.com/en/docs/catalog/advanced-trading-stocks-trading/api/ws-streams/market-streams#quote-stream Binance API Documentation}
     */
    public quoteStream(
        requestParameters: QuoteStreamRequest
    ): WebsocketStream<QuoteStreamResponse> {
        const stream = this.localVarParamCreator.quoteStream(
            requestParameters?.symbol,
            requestParameters?.id
        );

        return createStreamHandler<QuoteStreamResponse>(
            this.websocketBase,
            stream,
            requestParameters?.id
        );
    }

    /**
     * Per-symbol push whenever the tradable direction of a symbol changes. Pushed only when the value actually changes (new value ≠ old value). Also reachable via the SUBSCRIBE/UNSUBSCRIBE RPC — see [Subscribing via RPC](https://developers.binance.com/en/docs/products/stocks/websocket-streams-general-info#subscribing-via-rpc).
     *
     * @summary Tradability Stream
     * @param {TradabilityStreamRequest} requestParameters Request parameters.
     * @returns {WebsocketStream<TradabilityStreamResponse>}
     * @throws {RequiredError}
     * @memberof MarketStreamsApi
     * @see {@link https://developers.binance.com/en/docs/catalog/advanced-trading-stocks-trading/api/ws-streams/market-streams#tradability-stream Binance API Documentation}
     */
    public tradabilityStream(
        requestParameters: TradabilityStreamRequest
    ): WebsocketStream<TradabilityStreamResponse> {
        const stream = this.localVarParamCreator.tradabilityStream(
            requestParameters?.symbol,
            requestParameters?.id
        );

        return createStreamHandler<TradabilityStreamResponse>(
            this.websocketBase,
            stream,
            requestParameters?.id
        );
    }

    /**
     * Per-symbol trading-status transitions (halts, resumes, SSR, LULD pauses, etc.). Events that do not match a known status/reason rule are not pushed. Also reachable via the SUBSCRIBE/UNSUBSCRIBE RPC — see [Subscribing via RPC](https://developers.binance.com/en/docs/products/stocks/websocket-streams-general-info#subscribing-via-rpc).
     *
     * @summary Trading Status Stream
     * @param {TradingStatusStreamRequest} requestParameters Request parameters.
     * @returns {WebsocketStream<TradingStatusStreamResponse>}
     * @throws {RequiredError}
     * @memberof MarketStreamsApi
     * @see {@link https://developers.binance.com/en/docs/catalog/advanced-trading-stocks-trading/api/ws-streams/market-streams#trading-status-stream Binance API Documentation}
     */
    public tradingStatusStream(
        requestParameters: TradingStatusStreamRequest
    ): WebsocketStream<TradingStatusStreamResponse> {
        const stream = this.localVarParamCreator.tradingStatusStream(
            requestParameters?.symbol,
            requestParameters?.id
        );

        return createStreamHandler<TradingStatusStreamResponse>(
            this.websocketBase,
            stream,
            requestParameters?.id
        );
    }
}
