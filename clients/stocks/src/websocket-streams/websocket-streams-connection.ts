/**
 * Stocks Trading WebSocket Streams
 *
 * WebSocket stream definitions for Binance Stocks Trading. Base URL: wss://nbstream.binance.com/equity
 *
 * The version of the OpenAPI document: 1.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { WebsocketStreamsBase, WebsocketStream } from '@binance/common';
import { MarketStreamsApi } from './modules/market-streams-api';
import { UserStreamsApi } from './modules/user-streams-api';

import type {
    CalendarStreamRequest,
    KlineStreamRequest,
    PriceStreamRequest,
    QuoteStreamRequest,
    TradabilityStreamRequest,
    TradingStatusStreamRequest,
} from './modules/market-streams-api';
import type { OrderReportStreamRequest } from './modules/user-streams-api';

import type {
    CalendarStreamResponse,
    KlineStreamResponse,
    PriceStreamResponse,
    QuoteStreamResponse,
    TradabilityStreamResponse,
    TradingStatusStreamResponse,
} from './types';
import type { OrderReportStreamResponse } from './types';

export class WebsocketStreamsConnection {
    private websocketBase: WebsocketStreamsBase;
    private marketStreamsApi: MarketStreamsApi;
    private userStreamsApi: UserStreamsApi;

    constructor(websocketBase: WebsocketStreamsBase) {
        this.websocketBase = websocketBase;
        this.marketStreamsApi = new MarketStreamsApi(websocketBase);
        this.userStreamsApi = new UserStreamsApi(websocketBase);
    }

    /**
     * Adds an event listener for the specified WebSocket event.
     * @param event - The WebSocket event to listen for, such as 'open', 'message', 'error', 'close', 'ping', or 'pong'.
     * @param listener - The callback function to be executed when the event is triggered. The function can accept any number of arguments.
     */
    on(
        event: 'open' | 'message' | 'error' | 'close' | 'ping' | 'pong',
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        listener: (...args: any[]) => void
    ) {
        this.websocketBase.on(event, listener);
    }

    /**
     * Removes an event listener for the specified WebSocket event.
     * @param event - The WebSocket event to stop listening for, such as 'open', 'message', 'error', 'close', 'ping', or 'pong'.
     * @param listener - The callback function that was previously added as the event listener.
     */
    off(
        event: 'open' | 'message' | 'error' | 'close' | 'ping' | 'pong',
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        listener: (...args: any[]) => void
    ) {
        this.websocketBase.off(event, listener);
    }

    /**
     * Disconnects from the WebSocket server.
     * If there is no active connection, a warning is logged.
     * Otherwise, all connections in the connection pool are closed gracefully,
     * and a message is logged indicating that the connection has been disconnected.
     * @returns A Promise that resolves when all connections have been closed.
     * @throws Error if the WebSocket client is not set.
     */
    disconnect(): Promise<void> {
        return this.websocketBase.disconnect();
    }

    /**
     * Checks if the WebSocket connection is currently open.
     * @returns `true` if the connection is open, `false` otherwise.
     */
    isConnected(): boolean {
        return this.websocketBase.isConnected();
    }

    /**
     * Sends a ping message to all connected Websocket servers in the pool.
     * If no connections are ready, a warning is logged.
     * For each active connection, the ping message is sent, and debug logs provide details.
     * @throws Error if a Websocket client is not set for a connection.
     */
    pingServer(): void {
        this.websocketBase.pingServer();
    }

    /**
     * Subscribes to one or multiple WebSocket streams
     * Handles both single and pool modes
     * @param stream Single stream name or array of stream names to subscribe to
     * @param id Optional subscription ID
     * @returns void
     */
    subscribe(stream: string | string[], id?: string): void {
        this.websocketBase.subscribe(stream, id);
    }

    /**
     * Unsubscribes from one or multiple WebSocket streams
     * Handles both single and pool modes
     * @param stream Single stream name or array of stream names to unsubscribe from
     * @param id Optional unsubscription ID
     * @returns void
     */
    unsubscribe(stream: string | string[], id?: string): void {
        this.websocketBase.unsubscribe(stream, id);
    }

    /**
     * Checks if the WebSocket connection is subscribed to the specified stream.
     * @param stream The name of the WebSocket stream to check.
     * @returns `true` if the connection is subscribed to the stream, `false` otherwise.
     */
    isSubscribed(stream: string): boolean {
        return this.websocketBase.isSubscribed(stream);
    }

    /**
     * Single-stream broadcast of market-phase transitions. One message per transition; no periodic heartbeat payload. Server polls every 5 seconds. Also reachable via the SUBSCRIBE/UNSUBSCRIBE RPC — see [Subscribing via RPC](https://developers.binance.com/en/docs/products/stocks/websocket-streams-general-info#subscribing-via-rpc).
     *
     * @summary Calendar Stream
     * @param {CalendarStreamRequest} requestParameters Request parameters.
     *
     * @returns {WebsocketStream<CalendarStreamResponse>}
     * @throws {RequiredError}
     * @see {@link https://developers.binance.com/en/docs/catalog/advanced-trading-stocks-trading/api/ws-streams/market-streams#calendar-stream Binance API Documentation}
     */
    calendarStream(
        requestParameters: CalendarStreamRequest = {}
    ): WebsocketStream<CalendarStreamResponse> {
        return this.marketStreamsApi.calendarStream(requestParameters);
    }

    /**
     * Per-symbol candlestick updates. One stream per (symbol, interval) combination. Supported intervals: 5m, 1h, 1d, 1w, 1M. Also reachable via the SUBSCRIBE/UNSUBSCRIBE RPC — see [Subscribing via RPC](https://developers.binance.com/en/docs/products/stocks/websocket-streams-general-info#subscribing-via-rpc).
     *
     * @summary Kline Stream
     * @param {KlineStreamRequest} requestParameters Request parameters.
     *
     * @returns {WebsocketStream<KlineStreamResponse>}
     * @throws {RequiredError}
     * @see {@link https://developers.binance.com/en/docs/catalog/advanced-trading-stocks-trading/api/ws-streams/market-streams#kline-stream Binance API Documentation}
     */
    klineStream(requestParameters: KlineStreamRequest): WebsocketStream<KlineStreamResponse> {
        return this.marketStreamsApi.klineStream(requestParameters);
    }

    /**
     * Push-all price snapshot, polled every 3 seconds. One message carries the latest price for every active US-equity symbol. Also reachable via the SUBSCRIBE/UNSUBSCRIBE RPC — see [Subscribing via RPC](https://developers.binance.com/en/docs/products/stocks/websocket-streams-general-info#subscribing-via-rpc).
     *
     * @summary Price Stream
     * @param {PriceStreamRequest} requestParameters Request parameters.
     *
     * @returns {WebsocketStream<PriceStreamResponse>}
     * @throws {RequiredError}
     * @see {@link https://developers.binance.com/en/docs/catalog/advanced-trading-stocks-trading/api/ws-streams/market-streams#price-stream Binance API Documentation}
     */
    priceStream(requestParameters: PriceStreamRequest = {}): WebsocketStream<PriceStreamResponse> {
        return this.marketStreamsApi.priceStream(requestParameters);
    }

    /**
     * Per-symbol real-time best-bid / best-ask. Each symbol has its own stream. Per-symbol throttle: at most one push per symbol every 200 ms. Also reachable via the SUBSCRIBE/UNSUBSCRIBE RPC — see [Subscribing via RPC](https://developers.binance.com/en/docs/products/stocks/websocket-streams-general-info#subscribing-via-rpc).
     *
     * @summary Quote Stream
     * @param {QuoteStreamRequest} requestParameters Request parameters.
     *
     * @returns {WebsocketStream<QuoteStreamResponse>}
     * @throws {RequiredError}
     * @see {@link https://developers.binance.com/en/docs/catalog/advanced-trading-stocks-trading/api/ws-streams/market-streams#quote-stream Binance API Documentation}
     */
    quoteStream(requestParameters: QuoteStreamRequest): WebsocketStream<QuoteStreamResponse> {
        return this.marketStreamsApi.quoteStream(requestParameters);
    }

    /**
     * Per-symbol push whenever the tradable direction of a symbol changes. Pushed only when the value actually changes (new value ≠ old value). Also reachable via the SUBSCRIBE/UNSUBSCRIBE RPC — see [Subscribing via RPC](https://developers.binance.com/en/docs/products/stocks/websocket-streams-general-info#subscribing-via-rpc).
     *
     * @summary Tradability Stream
     * @param {TradabilityStreamRequest} requestParameters Request parameters.
     *
     * @returns {WebsocketStream<TradabilityStreamResponse>}
     * @throws {RequiredError}
     * @see {@link https://developers.binance.com/en/docs/catalog/advanced-trading-stocks-trading/api/ws-streams/market-streams#tradability-stream Binance API Documentation}
     */
    tradabilityStream(
        requestParameters: TradabilityStreamRequest
    ): WebsocketStream<TradabilityStreamResponse> {
        return this.marketStreamsApi.tradabilityStream(requestParameters);
    }

    /**
     * Per-symbol trading-status transitions (halts, resumes, SSR, LULD pauses, etc.). Events that do not match a known status/reason rule are not pushed. Also reachable via the SUBSCRIBE/UNSUBSCRIBE RPC — see [Subscribing via RPC](https://developers.binance.com/en/docs/products/stocks/websocket-streams-general-info#subscribing-via-rpc).
     *
     * @summary Trading Status Stream
     * @param {TradingStatusStreamRequest} requestParameters Request parameters.
     *
     * @returns {WebsocketStream<TradingStatusStreamResponse>}
     * @throws {RequiredError}
     * @see {@link https://developers.binance.com/en/docs/catalog/advanced-trading-stocks-trading/api/ws-streams/market-streams#trading-status-stream Binance API Documentation}
     */
    tradingStatusStream(
        requestParameters: TradingStatusStreamRequest
    ): WebsocketStream<TradingStatusStreamResponse> {
        return this.marketStreamsApi.tradingStatusStream(requestParameters);
    }

    /**
     * Real-time push of the authenticated user's order state transitions — both open-state updates (`ORDER_UPDATE`) and terminal-state notifications (`ORDER_TERMINAL`). Prerequisite: obtain a `listenKey` via the Listen Key endpoint first.
     *
     * @summary Order Report Stream
     * @param {OrderReportStreamRequest} requestParameters Request parameters.
     *
     * @returns {WebsocketStream<OrderReportStreamResponse>}
     * @throws {RequiredError}
     * @see {@link https://developers.binance.com/en/docs/catalog/advanced-trading-stocks-trading/api/ws-streams/user-streams#order-report-stream Binance API Documentation}
     */
    orderReportStream(
        requestParameters: OrderReportStreamRequest
    ): WebsocketStream<OrderReportStreamResponse> {
        return this.userStreamsApi.orderReportStream(requestParameters);
    }
}
