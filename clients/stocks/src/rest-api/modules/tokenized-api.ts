/**
 * Stocks Trading REST API
 *
 * REST APIs for Binance Stocks Trading. All endpoints under `/sapi/v1/equity/_*`.
 *
 * The version of the OpenAPI document: 1.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
import {
    ConfigurationRestAPI,
    TimeUnit,
    RestApiResponse,
    assertParamExists,
    sendRequest,
    type RequestArgs,
} from '@binance/common';
import type {
    TokenizedConvertHistoryResponse,
    TokenizedConvertStatusResponse,
    TokenizedMintResponse,
    TokenizedRedeemResponse,
} from '../types';

/**
 * TokenizedApi - axios parameter creator
 */
const TokenizedApiAxiosParamCreator = function (configuration: ConfigurationRestAPI) {
    return {
        /**
         * Paged history of mint / redeem conversions for the caller. Cursor-style pagination — supply the `nextLastId` from the previous page's response to fetch the next page.
         *
         * Weight: 1
         *
         * Security Type: USER_DATA
         *
         * @summary Tokenized Convert History (USER_DATA)
         * @param {number | bigint} [startTime] Start time (ms epoch).
         * @param {number | bigint} [endTime] End time (ms epoch).
         * @param {number | bigint} [lastId] Last record id from the previous page. Omit (or leave unset) to fetch the first page.
         * @param {number} [size] Page size. Default `20`, max `100`.
         * @param {number | bigint} [recvWindow] The value cannot be greater than `60000`.
         *
         * @throws {RequiredError}
         */
        tokenizedConvertHistory: async (
            startTime?: number | bigint,
            endTime?: number | bigint,
            lastId?: number | bigint,
            size?: number,
            recvWindow?: number | bigint
        ): Promise<RequestArgs> => {
            const localVarQueryParameter: Record<string, unknown> = {};
            const localVarBodyParameter: Record<string, unknown> = {};
            const localVarHeaderParameter: Record<string, unknown> = {};

            if (startTime !== undefined && startTime !== null) {
                localVarQueryParameter['startTime'] = startTime;
            }
            if (endTime !== undefined && endTime !== null) {
                localVarQueryParameter['endTime'] = endTime;
            }
            if (lastId !== undefined && lastId !== null) {
                localVarQueryParameter['lastId'] = lastId;
            }
            if (size !== undefined && size !== null) {
                localVarQueryParameter['size'] = size;
            }
            if (recvWindow !== undefined && recvWindow !== null) {
                localVarQueryParameter['recvWindow'] = recvWindow;
            }

            let _timeUnit: TimeUnit | undefined;
            if ('timeUnit' in configuration) _timeUnit = configuration.timeUnit as TimeUnit;

            return {
                endpoint: '/sapi/v1/equity/tokenized/history',
                method: 'GET',
                queryParams: localVarQueryParameter,
                bodyParams: localVarBodyParameter,
                headerParams: localVarHeaderParameter,
                timeUnit: _timeUnit,
            };
        },
        /**
         * Query the current status of a single mint / redeem request by its `issuerRequestId`. Returns an empty object when no record matches (caller's ID scope only).
         *
         * Weight: 1
         *
         * Security Type: USER_DATA
         *
         * @summary Tokenized Convert Status (USER_DATA)
         * @param {string} issuerRequestId Convert request id returned by `/tokenized/mint` or `/redeem`.
         * @param {TokenizedConvertStatusConvertTypeEnum} convertType `MINT` or `REDEEM`.
         * @param {number | bigint} [recvWindow] The value cannot be greater than `60000`.
         *
         * @throws {RequiredError}
         */
        tokenizedConvertStatus: async (
            issuerRequestId: string,
            convertType: TokenizedConvertStatusConvertTypeEnum,
            recvWindow?: number | bigint
        ): Promise<RequestArgs> => {
            // verify required parameter 'issuerRequestId' is not null or undefined
            assertParamExists('tokenizedConvertStatus', 'issuerRequestId', issuerRequestId);
            // verify required parameter 'convertType' is not null or undefined
            assertParamExists('tokenizedConvertStatus', 'convertType', convertType);

            const localVarQueryParameter: Record<string, unknown> = {};
            const localVarBodyParameter: Record<string, unknown> = {};
            const localVarHeaderParameter: Record<string, unknown> = {};

            if (issuerRequestId !== undefined && issuerRequestId !== null) {
                localVarQueryParameter['issuerRequestId'] = issuerRequestId;
            }
            if (convertType !== undefined && convertType !== null) {
                localVarQueryParameter['convertType'] = convertType;
            }
            if (recvWindow !== undefined && recvWindow !== null) {
                localVarQueryParameter['recvWindow'] = recvWindow;
            }

            let _timeUnit: TimeUnit | undefined;
            if ('timeUnit' in configuration) _timeUnit = configuration.timeUnit as TimeUnit;

            return {
                endpoint: '/sapi/v1/equity/tokenized/convert-status',
                method: 'GET',
                queryParams: localVarQueryParameter,
                bodyParams: localVarBodyParameter,
                headerParams: localVarHeaderParameter,
                timeUnit: _timeUnit,
            };
        },
        /**
         * Mint a tokenized asset from an underlying equity holding. The caller's underlying equity (e.g. `AAPL`) is burned (or locked), and the corresponding tokenized asset (e.g. `AAPLB`) is credited. The tokenized asset is resolved server-side from `underlyingAsset`; callers only provide the underlying ticker and quantity. Mint is asynchronous: the endpoint returns an `issuerRequestId` immediately with a transient status; poll `/tokenized/convert-status` to observe the terminal state, or inspect `/tokenized/history` for the full record. Rate limit: 50 requests / min (UID).
         *
         * Weight: 1
         *
         * Security Type: TRADE
         *
         * @summary Tokenized Mint (TRADE)
         * @param {string} underlyingAsset Underlying US-equity ticker, e.g. `AAPL`, `TSLA`. Resolved against the active-symbol list; unknown tickers return `-26004`. The target tokenized asset is looked up from this field via `/market/tokenized-assets`.
         * @param {string} underlyingAssetAmount Quantity of the underlying asset to mint from. Must be > 0.
         * @param {string} [clientOrderId] Client order id for idempotency. Format `^[a-zA-Z0-9-_]{32,36}$`. Auto-generated when omitted.
         * @param {number | bigint} [recvWindow] The value cannot be greater than `60000`.
         *
         * @throws {RequiredError}
         */
        tokenizedMint: async (
            underlyingAsset: string,
            underlyingAssetAmount: string,
            clientOrderId?: string,
            recvWindow?: number | bigint
        ): Promise<RequestArgs> => {
            // verify required parameter 'underlyingAsset' is not null or undefined
            assertParamExists('tokenizedMint', 'underlyingAsset', underlyingAsset);
            // verify required parameter 'underlyingAssetAmount' is not null or undefined
            assertParamExists('tokenizedMint', 'underlyingAssetAmount', underlyingAssetAmount);

            const localVarQueryParameter: Record<string, unknown> = {};
            const localVarBodyParameter: Record<string, unknown> = {};
            const localVarHeaderParameter: Record<string, unknown> = {};

            if (underlyingAsset !== undefined && underlyingAsset !== null) {
                localVarQueryParameter['underlyingAsset'] = underlyingAsset;
            }
            if (underlyingAssetAmount !== undefined && underlyingAssetAmount !== null) {
                localVarQueryParameter['underlyingAssetAmount'] = underlyingAssetAmount;
            }
            if (clientOrderId !== undefined && clientOrderId !== null) {
                localVarQueryParameter['clientOrderId'] = clientOrderId;
            }
            if (recvWindow !== undefined && recvWindow !== null) {
                localVarQueryParameter['recvWindow'] = recvWindow;
            }

            let _timeUnit: TimeUnit | undefined;
            if ('timeUnit' in configuration) _timeUnit = configuration.timeUnit as TimeUnit;

            return {
                endpoint: '/sapi/v1/equity/tokenized/mint',
                method: 'POST',
                queryParams: localVarQueryParameter,
                bodyParams: localVarBodyParameter,
                headerParams: localVarHeaderParameter,
                timeUnit: _timeUnit,
            };
        },
        /**
         * Redeem a tokenized asset back into the underlying equity. The caller's tokenized asset (e.g. `AAPLB`) is burned, and the corresponding underlying equity (e.g. `AAPL`) is released. The underlying asset is resolved server-side from `tokenizedAsset`; callers only provide the tokenized asset and quantity. Redeem is asynchronous: the endpoint returns an `issuerRequestId` immediately with a transient status; poll `/tokenized/convert-status` to observe the terminal state, or inspect `/tokenized/history` for the full record. Rate limit: 200 requests / min (UID).
         *
         * Weight: 1
         *
         * Security Type: TRADE
         *
         * @summary Tokenized Redeem (TRADE)
         * @param {string} tokenizedAsset Tokenized asset to redeem, e.g. `AAPLB`. Not a US-equity ticker — this is the on-chain tokenized asset identifier. Unknown asset returns `-1102` (the message currently says the parameter was empty/malformed, but it was in fact sent — it is simply unknown). The target underlying ticker is looked up from this field via `/market/tokenized-assets`.
         * @param {string} tokenizedAssetAmount Quantity of the tokenized asset to redeem. Must be > 0.
         * @param {string} [clientOrderId] Client order id for idempotency. Format `^[a-zA-Z0-9-_]{32,36}$`. Auto-generated when omitted.
         * @param {number | bigint} [recvWindow] The value cannot be greater than `60000`.
         *
         * @throws {RequiredError}
         */
        tokenizedRedeem: async (
            tokenizedAsset: string,
            tokenizedAssetAmount: string,
            clientOrderId?: string,
            recvWindow?: number | bigint
        ): Promise<RequestArgs> => {
            // verify required parameter 'tokenizedAsset' is not null or undefined
            assertParamExists('tokenizedRedeem', 'tokenizedAsset', tokenizedAsset);
            // verify required parameter 'tokenizedAssetAmount' is not null or undefined
            assertParamExists('tokenizedRedeem', 'tokenizedAssetAmount', tokenizedAssetAmount);

            const localVarQueryParameter: Record<string, unknown> = {};
            const localVarBodyParameter: Record<string, unknown> = {};
            const localVarHeaderParameter: Record<string, unknown> = {};

            if (tokenizedAsset !== undefined && tokenizedAsset !== null) {
                localVarQueryParameter['tokenizedAsset'] = tokenizedAsset;
            }
            if (tokenizedAssetAmount !== undefined && tokenizedAssetAmount !== null) {
                localVarQueryParameter['tokenizedAssetAmount'] = tokenizedAssetAmount;
            }
            if (clientOrderId !== undefined && clientOrderId !== null) {
                localVarQueryParameter['clientOrderId'] = clientOrderId;
            }
            if (recvWindow !== undefined && recvWindow !== null) {
                localVarQueryParameter['recvWindow'] = recvWindow;
            }

            let _timeUnit: TimeUnit | undefined;
            if ('timeUnit' in configuration) _timeUnit = configuration.timeUnit as TimeUnit;

            return {
                endpoint: '/sapi/v1/equity/tokenized/redeem',
                method: 'POST',
                queryParams: localVarQueryParameter,
                bodyParams: localVarBodyParameter,
                headerParams: localVarHeaderParameter,
                timeUnit: _timeUnit,
            };
        },
    };
};

/**
 * TokenizedApi - interface
 * @interface TokenizedApi
 */
export interface TokenizedApiInterface {
    /**
     * Paged history of mint / redeem conversions for the caller. Cursor-style pagination — supply the `nextLastId` from the previous page's response to fetch the next page.
     *
     * Weight: 1
     *
     * Security Type: USER_DATA
     *
     * @summary Tokenized Convert History (USER_DATA)
     * @param {TokenizedConvertHistoryRequest} requestParameters Request parameters.
     *
     * @throws {RequiredError | ConnectorClientError | UnauthorizedError | ForbiddenError | TooManyRequestsError | RateLimitBanError | ServerError | NotFoundError | NetworkError | BadRequestError}
     * @memberof TokenizedApiInterface
     */
    tokenizedConvertHistory(
        requestParameters?: TokenizedConvertHistoryRequest
    ): Promise<RestApiResponse<TokenizedConvertHistoryResponse>>;
    /**
     * Query the current status of a single mint / redeem request by its `issuerRequestId`. Returns an empty object when no record matches (caller's ID scope only).
     *
     * Weight: 1
     *
     * Security Type: USER_DATA
     *
     * @summary Tokenized Convert Status (USER_DATA)
     * @param {TokenizedConvertStatusRequest} requestParameters Request parameters.
     *
     * @throws {RequiredError | ConnectorClientError | UnauthorizedError | ForbiddenError | TooManyRequestsError | RateLimitBanError | ServerError | NotFoundError | NetworkError | BadRequestError}
     * @memberof TokenizedApiInterface
     */
    tokenizedConvertStatus(
        requestParameters: TokenizedConvertStatusRequest
    ): Promise<RestApiResponse<TokenizedConvertStatusResponse>>;
    /**
     * Mint a tokenized asset from an underlying equity holding. The caller's underlying equity (e.g. `AAPL`) is burned (or locked), and the corresponding tokenized asset (e.g. `AAPLB`) is credited. The tokenized asset is resolved server-side from `underlyingAsset`; callers only provide the underlying ticker and quantity. Mint is asynchronous: the endpoint returns an `issuerRequestId` immediately with a transient status; poll `/tokenized/convert-status` to observe the terminal state, or inspect `/tokenized/history` for the full record. Rate limit: 50 requests / min (UID).
     *
     * Weight: 1
     *
     * Security Type: TRADE
     *
     * @summary Tokenized Mint (TRADE)
     * @param {TokenizedMintRequest} requestParameters Request parameters.
     *
     * @throws {RequiredError | ConnectorClientError | UnauthorizedError | ForbiddenError | TooManyRequestsError | RateLimitBanError | ServerError | NotFoundError | NetworkError | BadRequestError}
     * @memberof TokenizedApiInterface
     */
    tokenizedMint(
        requestParameters: TokenizedMintRequest
    ): Promise<RestApiResponse<TokenizedMintResponse>>;
    /**
     * Redeem a tokenized asset back into the underlying equity. The caller's tokenized asset (e.g. `AAPLB`) is burned, and the corresponding underlying equity (e.g. `AAPL`) is released. The underlying asset is resolved server-side from `tokenizedAsset`; callers only provide the tokenized asset and quantity. Redeem is asynchronous: the endpoint returns an `issuerRequestId` immediately with a transient status; poll `/tokenized/convert-status` to observe the terminal state, or inspect `/tokenized/history` for the full record. Rate limit: 200 requests / min (UID).
     *
     * Weight: 1
     *
     * Security Type: TRADE
     *
     * @summary Tokenized Redeem (TRADE)
     * @param {TokenizedRedeemRequest} requestParameters Request parameters.
     *
     * @throws {RequiredError | ConnectorClientError | UnauthorizedError | ForbiddenError | TooManyRequestsError | RateLimitBanError | ServerError | NotFoundError | NetworkError | BadRequestError}
     * @memberof TokenizedApiInterface
     */
    tokenizedRedeem(
        requestParameters: TokenizedRedeemRequest
    ): Promise<RestApiResponse<TokenizedRedeemResponse>>;
}

/**
 * Request parameters for tokenizedConvertHistory operation in TokenizedApi.
 * @interface TokenizedConvertHistoryRequest
 */
export interface TokenizedConvertHistoryRequest {
    /**
     * Start time (ms epoch).
     * @type {number | bigint}
     * @memberof TokenizedApiTokenizedConvertHistory
     */
    readonly startTime?: number | bigint;

    /**
     * End time (ms epoch).
     * @type {number | bigint}
     * @memberof TokenizedApiTokenizedConvertHistory
     */
    readonly endTime?: number | bigint;

    /**
     * Last record id from the previous page. Omit (or leave unset) to fetch the first page.
     * @type {number | bigint}
     * @memberof TokenizedApiTokenizedConvertHistory
     */
    readonly lastId?: number | bigint;

    /**
     * Page size. Default `20`, max `100`.
     * @type {number}
     * @memberof TokenizedApiTokenizedConvertHistory
     */
    readonly size?: number;

    /**
     * The value cannot be greater than `60000`.
     * @type {number | bigint}
     * @memberof TokenizedApiTokenizedConvertHistory
     */
    readonly recvWindow?: number | bigint;
}

/**
 * Request parameters for tokenizedConvertStatus operation in TokenizedApi.
 * @interface TokenizedConvertStatusRequest
 */
export interface TokenizedConvertStatusRequest {
    /**
     * Convert request id returned by `/tokenized/mint` or `/redeem`.
     * @type {string}
     * @memberof TokenizedApiTokenizedConvertStatus
     */
    readonly issuerRequestId: string;

    /**
     * `MINT` or `REDEEM`.
     * @type {'MINT' | 'REDEEM'}
     * @memberof TokenizedApiTokenizedConvertStatus
     */
    readonly convertType: TokenizedConvertStatusConvertTypeEnum;

    /**
     * The value cannot be greater than `60000`.
     * @type {number | bigint}
     * @memberof TokenizedApiTokenizedConvertStatus
     */
    readonly recvWindow?: number | bigint;
}

/**
 * Request parameters for tokenizedMint operation in TokenizedApi.
 * @interface TokenizedMintRequest
 */
export interface TokenizedMintRequest {
    /**
     * Underlying US-equity ticker, e.g. `AAPL`, `TSLA`. Resolved against the active-symbol list; unknown tickers return `-26004`. The target tokenized asset is looked up from this field via `/market/tokenized-assets`.
     * @type {string}
     * @memberof TokenizedApiTokenizedMint
     */
    readonly underlyingAsset: string;

    /**
     * Quantity of the underlying asset to mint from. Must be > 0.
     * @type {string}
     * @memberof TokenizedApiTokenizedMint
     */
    readonly underlyingAssetAmount: string;

    /**
     * Client order id for idempotency. Format `^[a-zA-Z0-9-_]{32,36}$`. Auto-generated when omitted.
     * @type {string}
     * @memberof TokenizedApiTokenizedMint
     */
    readonly clientOrderId?: string;

    /**
     * The value cannot be greater than `60000`.
     * @type {number | bigint}
     * @memberof TokenizedApiTokenizedMint
     */
    readonly recvWindow?: number | bigint;
}

/**
 * Request parameters for tokenizedRedeem operation in TokenizedApi.
 * @interface TokenizedRedeemRequest
 */
export interface TokenizedRedeemRequest {
    /**
     * Tokenized asset to redeem, e.g. `AAPLB`. Not a US-equity ticker — this is the on-chain tokenized asset identifier. Unknown asset returns `-1102` (the message currently says the parameter was empty/malformed, but it was in fact sent — it is simply unknown). The target underlying ticker is looked up from this field via `/market/tokenized-assets`.
     * @type {string}
     * @memberof TokenizedApiTokenizedRedeem
     */
    readonly tokenizedAsset: string;

    /**
     * Quantity of the tokenized asset to redeem. Must be > 0.
     * @type {string}
     * @memberof TokenizedApiTokenizedRedeem
     */
    readonly tokenizedAssetAmount: string;

    /**
     * Client order id for idempotency. Format `^[a-zA-Z0-9-_]{32,36}$`. Auto-generated when omitted.
     * @type {string}
     * @memberof TokenizedApiTokenizedRedeem
     */
    readonly clientOrderId?: string;

    /**
     * The value cannot be greater than `60000`.
     * @type {number | bigint}
     * @memberof TokenizedApiTokenizedRedeem
     */
    readonly recvWindow?: number | bigint;
}

/**
 * TokenizedApi - object-oriented interface
 * @class TokenizedApi
 */
export class TokenizedApi implements TokenizedApiInterface {
    private readonly configuration: ConfigurationRestAPI;
    private localVarAxiosParamCreator;

    constructor(configuration: ConfigurationRestAPI) {
        this.configuration = configuration;
        this.localVarAxiosParamCreator = TokenizedApiAxiosParamCreator(configuration);
    }

    /**
     * Paged history of mint / redeem conversions for the caller. Cursor-style pagination — supply the `nextLastId` from the previous page's response to fetch the next page.
     *
     * Weight: 1
     *
     * Security Type: USER_DATA
     *
     * @summary Tokenized Convert History (USER_DATA)
     * @param {TokenizedConvertHistoryRequest} requestParameters Request parameters.
     * @returns {Promise<RestApiResponse<TokenizedConvertHistoryResponse>>}
     * @throws {RequiredError | ConnectorClientError | UnauthorizedError | ForbiddenError | TooManyRequestsError | RateLimitBanError | ServerError | NotFoundError | NetworkError | BadRequestError}
     * @memberof TokenizedApi
     * @see {@link https://developers.binance.com/en/docs/catalog/advanced-trading-stocks-trading/api/rest-api/tokenized#tokenized-convert-history Binance API Documentation}
     */
    public async tokenizedConvertHistory(
        requestParameters: TokenizedConvertHistoryRequest = {}
    ): Promise<RestApiResponse<TokenizedConvertHistoryResponse>> {
        const localVarAxiosArgs = await this.localVarAxiosParamCreator.tokenizedConvertHistory(
            requestParameters?.startTime,
            requestParameters?.endTime,
            requestParameters?.lastId,
            requestParameters?.size,
            requestParameters?.recvWindow
        );
        return sendRequest<TokenizedConvertHistoryResponse>(
            this.configuration,
            localVarAxiosArgs.endpoint,
            localVarAxiosArgs.method,
            localVarAxiosArgs.queryParams,
            localVarAxiosArgs.bodyParams,
            localVarAxiosArgs.headerParams,
            localVarAxiosArgs?.timeUnit,
            { isSigned: true }
        );
    }

    /**
     * Query the current status of a single mint / redeem request by its `issuerRequestId`. Returns an empty object when no record matches (caller's ID scope only).
     *
     * Weight: 1
     *
     * Security Type: USER_DATA
     *
     * @summary Tokenized Convert Status (USER_DATA)
     * @param {TokenizedConvertStatusRequest} requestParameters Request parameters.
     * @returns {Promise<RestApiResponse<TokenizedConvertStatusResponse>>}
     * @throws {RequiredError | ConnectorClientError | UnauthorizedError | ForbiddenError | TooManyRequestsError | RateLimitBanError | ServerError | NotFoundError | NetworkError | BadRequestError}
     * @memberof TokenizedApi
     * @see {@link https://developers.binance.com/en/docs/catalog/advanced-trading-stocks-trading/api/rest-api/tokenized#tokenized-convert-status Binance API Documentation}
     */
    public async tokenizedConvertStatus(
        requestParameters: TokenizedConvertStatusRequest
    ): Promise<RestApiResponse<TokenizedConvertStatusResponse>> {
        const localVarAxiosArgs = await this.localVarAxiosParamCreator.tokenizedConvertStatus(
            requestParameters?.issuerRequestId,
            requestParameters?.convertType,
            requestParameters?.recvWindow
        );
        return sendRequest<TokenizedConvertStatusResponse>(
            this.configuration,
            localVarAxiosArgs.endpoint,
            localVarAxiosArgs.method,
            localVarAxiosArgs.queryParams,
            localVarAxiosArgs.bodyParams,
            localVarAxiosArgs.headerParams,
            localVarAxiosArgs?.timeUnit,
            { isSigned: true }
        );
    }

    /**
     * Mint a tokenized asset from an underlying equity holding. The caller's underlying equity (e.g. `AAPL`) is burned (or locked), and the corresponding tokenized asset (e.g. `AAPLB`) is credited. The tokenized asset is resolved server-side from `underlyingAsset`; callers only provide the underlying ticker and quantity. Mint is asynchronous: the endpoint returns an `issuerRequestId` immediately with a transient status; poll `/tokenized/convert-status` to observe the terminal state, or inspect `/tokenized/history` for the full record. Rate limit: 50 requests / min (UID).
     *
     * Weight: 1
     *
     * Security Type: TRADE
     *
     * @summary Tokenized Mint (TRADE)
     * @param {TokenizedMintRequest} requestParameters Request parameters.
     * @returns {Promise<RestApiResponse<TokenizedMintResponse>>}
     * @throws {RequiredError | ConnectorClientError | UnauthorizedError | ForbiddenError | TooManyRequestsError | RateLimitBanError | ServerError | NotFoundError | NetworkError | BadRequestError}
     * @memberof TokenizedApi
     * @see {@link https://developers.binance.com/en/docs/catalog/advanced-trading-stocks-trading/api/rest-api/tokenized#tokenized-mint Binance API Documentation}
     */
    public async tokenizedMint(
        requestParameters: TokenizedMintRequest
    ): Promise<RestApiResponse<TokenizedMintResponse>> {
        const localVarAxiosArgs = await this.localVarAxiosParamCreator.tokenizedMint(
            requestParameters?.underlyingAsset,
            requestParameters?.underlyingAssetAmount,
            requestParameters?.clientOrderId,
            requestParameters?.recvWindow
        );
        return sendRequest<TokenizedMintResponse>(
            this.configuration,
            localVarAxiosArgs.endpoint,
            localVarAxiosArgs.method,
            localVarAxiosArgs.queryParams,
            localVarAxiosArgs.bodyParams,
            localVarAxiosArgs.headerParams,
            localVarAxiosArgs?.timeUnit,
            { isSigned: true }
        );
    }

    /**
     * Redeem a tokenized asset back into the underlying equity. The caller's tokenized asset (e.g. `AAPLB`) is burned, and the corresponding underlying equity (e.g. `AAPL`) is released. The underlying asset is resolved server-side from `tokenizedAsset`; callers only provide the tokenized asset and quantity. Redeem is asynchronous: the endpoint returns an `issuerRequestId` immediately with a transient status; poll `/tokenized/convert-status` to observe the terminal state, or inspect `/tokenized/history` for the full record. Rate limit: 200 requests / min (UID).
     *
     * Weight: 1
     *
     * Security Type: TRADE
     *
     * @summary Tokenized Redeem (TRADE)
     * @param {TokenizedRedeemRequest} requestParameters Request parameters.
     * @returns {Promise<RestApiResponse<TokenizedRedeemResponse>>}
     * @throws {RequiredError | ConnectorClientError | UnauthorizedError | ForbiddenError | TooManyRequestsError | RateLimitBanError | ServerError | NotFoundError | NetworkError | BadRequestError}
     * @memberof TokenizedApi
     * @see {@link https://developers.binance.com/en/docs/catalog/advanced-trading-stocks-trading/api/rest-api/tokenized#tokenized-redeem Binance API Documentation}
     */
    public async tokenizedRedeem(
        requestParameters: TokenizedRedeemRequest
    ): Promise<RestApiResponse<TokenizedRedeemResponse>> {
        const localVarAxiosArgs = await this.localVarAxiosParamCreator.tokenizedRedeem(
            requestParameters?.tokenizedAsset,
            requestParameters?.tokenizedAssetAmount,
            requestParameters?.clientOrderId,
            requestParameters?.recvWindow
        );
        return sendRequest<TokenizedRedeemResponse>(
            this.configuration,
            localVarAxiosArgs.endpoint,
            localVarAxiosArgs.method,
            localVarAxiosArgs.queryParams,
            localVarAxiosArgs.bodyParams,
            localVarAxiosArgs.headerParams,
            localVarAxiosArgs?.timeUnit,
            { isSigned: true }
        );
    }
}

export enum TokenizedConvertStatusConvertTypeEnum {
    MINT = 'MINT',
    REDEEM = 'REDEEM',
}
