/**
 * Stocks Trading WebSocket Streams
 *
 * WebSocket stream definitions for Binance Stocks Trading. Base URL: wss://nbstream.binance.com/equity
 *
 * The version of the OpenAPI document: 1.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { jest, expect, describe, it } from '@jest/globals';
import { JSONParse, JSONStringify } from 'json-with-bigint';
import {
    ConfigurationWebsocketStreams,
    WebsocketStreamsBase,
    replaceWebsocketStreamsPlaceholders,
} from '@binance/common';
import {
    CalendarStreamRequest,
    KlineStreamRequest,
    PriceStreamRequest,
    QuoteStreamRequest,
    TradabilityStreamRequest,
    TradingStatusStreamRequest,
} from '../../../src/websocket-streams';
import { MarketStreamsApi } from '../../../src/websocket-streams';
import { mockSubscription } from './utils';

describe('MarketStreamsApi', () => {
    describe('calendarStream()', () => {
        it('should execute calendarStream() successfully', async () => {
            const params: CalendarStreamRequest = {
                id: 'e9d6b4349871b40611412680b3445fac',
            };

            const mockResponse = JSONParse(
                JSONStringify({
                    e: 'calendar',
                    from: 'PRE_MARKET',
                    to: 'MARKET_OPEN',
                    ts: 1710334200000,
                })
            );

            mockSubscription(
                `ws/${replaceWebsocketStreamsPlaceholders('/calendar'.slice(1), params as unknown as Record<string, CalendarStreamRequest>)}`,
                mockResponse
            );
        });

        it('should handle calendarStream() WebSocket stream data', () => {
            const configuration = new ConfigurationWebsocketStreams({});
            const websocketStreamClient = new WebsocketStreamsBase(configuration);
            const websocketStreamApi = new MarketStreamsApi(websocketStreamClient);

            const params: CalendarStreamRequest = {
                id: 'e9d6b4349871b40611412680b3445fac',
            };

            const mockResponse = JSONParse(
                JSONStringify({
                    e: 'calendar',
                    from: 'PRE_MARKET',
                    to: 'MARKET_OPEN',
                    ts: 1710334200000,
                })
            );

            const stream = websocketStreamApi.calendarStream(params);
            const mockCallback = jest.fn(() => {});
            stream.on('message', mockCallback);

            websocketStreamClient['onMessage'](
                JSONStringify({
                    stream: replaceWebsocketStreamsPlaceholders(
                        '/calendar'.slice(1),
                        params as unknown as Record<string, CalendarStreamRequest>
                    ),
                    data: mockResponse,
                }),
                websocketStreamClient.connectionPool[0]
            );

            expect(mockCallback).toHaveBeenCalledWith(mockResponse);
        });
    });

    describe('klineStream()', () => {
        it('should execute klineStream() successfully', async () => {
            const params: KlineStreamRequest = {
                symbol: 'AAPL',
                interval: '5m',
                id: 'e9d6b4349871b40611412680b3445fac',
            };

            const mockResponse = JSONParse(
                JSONStringify({
                    e: 'kline',
                    E: 1710320400000,
                    s: 'AAPL',
                    k: {
                        t: 1710316800000,
                        ct: 1710317099999,
                        s: 'AAPL',
                        i: '5m',
                        o: '182.50',
                        c: '183.20',
                        h: '183.50',
                        l: '182.30',
                        v: '15000',
                        n: 120,
                        x: false,
                        vw: '182.95',
                    },
                })
            );

            mockSubscription(
                `ws/${replaceWebsocketStreamsPlaceholders('/<symbol>@kline_<interval>'.slice(1), params as unknown as Record<string, KlineStreamRequest>)}`,
                mockResponse
            );
        });

        it('should handle klineStream() WebSocket stream data', () => {
            const configuration = new ConfigurationWebsocketStreams({});
            const websocketStreamClient = new WebsocketStreamsBase(configuration);
            const websocketStreamApi = new MarketStreamsApi(websocketStreamClient);

            const params: KlineStreamRequest = {
                symbol: 'AAPL',
                interval: '5m',
                id: 'e9d6b4349871b40611412680b3445fac',
            };

            const mockResponse = JSONParse(
                JSONStringify({
                    e: 'kline',
                    E: 1710320400000,
                    s: 'AAPL',
                    k: {
                        t: 1710316800000,
                        ct: 1710317099999,
                        s: 'AAPL',
                        i: '5m',
                        o: '182.50',
                        c: '183.20',
                        h: '183.50',
                        l: '182.30',
                        v: '15000',
                        n: 120,
                        x: false,
                        vw: '182.95',
                    },
                })
            );

            const stream = websocketStreamApi.klineStream(params);
            const mockCallback = jest.fn(() => {});
            stream.on('message', mockCallback);

            websocketStreamClient['onMessage'](
                JSONStringify({
                    stream: replaceWebsocketStreamsPlaceholders(
                        '/<symbol>@kline_<interval>'.slice(1),
                        params as unknown as Record<string, KlineStreamRequest>
                    ),
                    data: mockResponse,
                }),
                websocketStreamClient.connectionPool[0]
            );

            expect(mockCallback).toHaveBeenCalledWith(mockResponse);
        });

        it('should throw RequiredError when symbol is missing', () => {
            const configuration = new ConfigurationWebsocketStreams({});
            const websocketStreamClient = new WebsocketStreamsBase(configuration);
            const websocketStreamApi = new MarketStreamsApi(websocketStreamClient);

            const _params: KlineStreamRequest = {
                symbol: 'AAPL',
                interval: '5m',
            };
            const params = Object.assign({ ..._params });
            delete params?.symbol;

            expect(() => websocketStreamApi.klineStream(params)).toThrow(
                'Required parameter symbol was null or undefined when calling klineStream.'
            );
        });

        it('should throw RequiredError when interval is missing', () => {
            const configuration = new ConfigurationWebsocketStreams({});
            const websocketStreamClient = new WebsocketStreamsBase(configuration);
            const websocketStreamApi = new MarketStreamsApi(websocketStreamClient);

            const _params: KlineStreamRequest = {
                symbol: 'AAPL',
                interval: '5m',
            };
            const params = Object.assign({ ..._params });
            delete params?.interval;

            expect(() => websocketStreamApi.klineStream(params)).toThrow(
                'Required parameter interval was null or undefined when calling klineStream.'
            );
        });
    });

    describe('priceStream()', () => {
        it('should execute priceStream() successfully', async () => {
            const params: PriceStreamRequest = {
                id: 'e9d6b4349871b40611412680b3445fac',
            };

            const mockResponse = JSONParse(
                JSONStringify({
                    e: 'price',
                    rates: [
                        {
                            s: 'AAPL',
                            ac: 'EQ_AAPL',
                            p: '182.5',
                            t: 1710320400000,
                            pc: '181.2000',
                            tc: '182.0000',
                            mp: 'O',
                        },
                    ],
                })
            );

            mockSubscription(
                `ws/${replaceWebsocketStreamsPlaceholders('/price'.slice(1), params as unknown as Record<string, PriceStreamRequest>)}`,
                mockResponse
            );
        });

        it('should handle priceStream() WebSocket stream data', () => {
            const configuration = new ConfigurationWebsocketStreams({});
            const websocketStreamClient = new WebsocketStreamsBase(configuration);
            const websocketStreamApi = new MarketStreamsApi(websocketStreamClient);

            const params: PriceStreamRequest = {
                id: 'e9d6b4349871b40611412680b3445fac',
            };

            const mockResponse = JSONParse(
                JSONStringify({
                    e: 'price',
                    rates: [
                        {
                            s: 'AAPL',
                            ac: 'EQ_AAPL',
                            p: '182.5',
                            t: 1710320400000,
                            pc: '181.2000',
                            tc: '182.0000',
                            mp: 'O',
                        },
                    ],
                })
            );

            const stream = websocketStreamApi.priceStream(params);
            const mockCallback = jest.fn(() => {});
            stream.on('message', mockCallback);

            websocketStreamClient['onMessage'](
                JSONStringify({
                    stream: replaceWebsocketStreamsPlaceholders(
                        '/price'.slice(1),
                        params as unknown as Record<string, PriceStreamRequest>
                    ),
                    data: mockResponse,
                }),
                websocketStreamClient.connectionPool[0]
            );

            expect(mockCallback).toHaveBeenCalledWith(mockResponse);
        });
    });

    describe('quoteStream()', () => {
        it('should execute quoteStream() successfully', async () => {
            const params: QuoteStreamRequest = {
                symbol: 'AAPL',
                id: 'e9d6b4349871b40611412680b3445fac',
            };

            const mockResponse = JSONParse(
                JSONStringify({
                    e: 'quote',
                    E: 1710320400000,
                    s: 'AAPL',
                    bp: '182.50',
                    ap: '182.55',
                    bs: 100,
                    as: 200,
                    T: 1710320399500,
                })
            );

            mockSubscription(
                `ws/${replaceWebsocketStreamsPlaceholders('/<symbol>@quote'.slice(1), params as unknown as Record<string, QuoteStreamRequest>)}`,
                mockResponse
            );
        });

        it('should handle quoteStream() WebSocket stream data', () => {
            const configuration = new ConfigurationWebsocketStreams({});
            const websocketStreamClient = new WebsocketStreamsBase(configuration);
            const websocketStreamApi = new MarketStreamsApi(websocketStreamClient);

            const params: QuoteStreamRequest = {
                symbol: 'AAPL',
                id: 'e9d6b4349871b40611412680b3445fac',
            };

            const mockResponse = JSONParse(
                JSONStringify({
                    e: 'quote',
                    E: 1710320400000,
                    s: 'AAPL',
                    bp: '182.50',
                    ap: '182.55',
                    bs: 100,
                    as: 200,
                    T: 1710320399500,
                })
            );

            const stream = websocketStreamApi.quoteStream(params);
            const mockCallback = jest.fn(() => {});
            stream.on('message', mockCallback);

            websocketStreamClient['onMessage'](
                JSONStringify({
                    stream: replaceWebsocketStreamsPlaceholders(
                        '/<symbol>@quote'.slice(1),
                        params as unknown as Record<string, QuoteStreamRequest>
                    ),
                    data: mockResponse,
                }),
                websocketStreamClient.connectionPool[0]
            );

            expect(mockCallback).toHaveBeenCalledWith(mockResponse);
        });

        it('should throw RequiredError when symbol is missing', () => {
            const configuration = new ConfigurationWebsocketStreams({});
            const websocketStreamClient = new WebsocketStreamsBase(configuration);
            const websocketStreamApi = new MarketStreamsApi(websocketStreamClient);

            const _params: QuoteStreamRequest = {
                symbol: 'AAPL',
            };
            const params = Object.assign({ ..._params });
            delete params?.symbol;

            expect(() => websocketStreamApi.quoteStream(params)).toThrow(
                'Required parameter symbol was null or undefined when calling quoteStream.'
            );
        });
    });

    describe('tradabilityStream()', () => {
        it('should execute tradabilityStream() successfully', async () => {
            const params: TradabilityStreamRequest = {
                symbol: 'AAPL',
                id: 'e9d6b4349871b40611412680b3445fac',
            };

            const mockResponse = JSONParse(
                JSONStringify({
                    e: 'tradability',
                    symbol: 'AAPL',
                    assetCode: 'EQ_AAPL',
                    tradability: 'BUY_SELL',
                    t: 1712345678000,
                })
            );

            mockSubscription(
                `ws/${replaceWebsocketStreamsPlaceholders('/<symbol>@tradability'.slice(1), params as unknown as Record<string, TradabilityStreamRequest>)}`,
                mockResponse
            );
        });

        it('should handle tradabilityStream() WebSocket stream data', () => {
            const configuration = new ConfigurationWebsocketStreams({});
            const websocketStreamClient = new WebsocketStreamsBase(configuration);
            const websocketStreamApi = new MarketStreamsApi(websocketStreamClient);

            const params: TradabilityStreamRequest = {
                symbol: 'AAPL',
                id: 'e9d6b4349871b40611412680b3445fac',
            };

            const mockResponse = JSONParse(
                JSONStringify({
                    e: 'tradability',
                    symbol: 'AAPL',
                    assetCode: 'EQ_AAPL',
                    tradability: 'BUY_SELL',
                    t: 1712345678000,
                })
            );

            const stream = websocketStreamApi.tradabilityStream(params);
            const mockCallback = jest.fn(() => {});
            stream.on('message', mockCallback);

            websocketStreamClient['onMessage'](
                JSONStringify({
                    stream: replaceWebsocketStreamsPlaceholders(
                        '/<symbol>@tradability'.slice(1),
                        params as unknown as Record<string, TradabilityStreamRequest>
                    ),
                    data: mockResponse,
                }),
                websocketStreamClient.connectionPool[0]
            );

            expect(mockCallback).toHaveBeenCalledWith(mockResponse);
        });

        it('should throw RequiredError when symbol is missing', () => {
            const configuration = new ConfigurationWebsocketStreams({});
            const websocketStreamClient = new WebsocketStreamsBase(configuration);
            const websocketStreamApi = new MarketStreamsApi(websocketStreamClient);

            const _params: TradabilityStreamRequest = {
                symbol: 'AAPL',
            };
            const params = Object.assign({ ..._params });
            delete params?.symbol;

            expect(() => websocketStreamApi.tradabilityStream(params)).toThrow(
                'Required parameter symbol was null or undefined when calling tradabilityStream.'
            );
        });
    });

    describe('tradingStatusStream()', () => {
        it('should execute tradingStatusStream() successfully', async () => {
            const params: TradingStatusStreamRequest = {
                symbol: 'AAPL',
                id: 'e9d6b4349871b40611412680b3445fac',
            };

            const mockResponse = JSONParse(
                JSONStringify({
                    e: 'tradingStatus',
                    symbol: 'AAPL',
                    assetCode: 'EQ_AAPL',
                    status: 'TRADING_HALTED',
                    msg: 'MATERIAL_NEWS',
                    time: 1614021300000,
                    z: 'C',
                    tradability: 'NONE',
                })
            );

            mockSubscription(
                `ws/${replaceWebsocketStreamsPlaceholders('/<symbol>@tradingStatus'.slice(1), params as unknown as Record<string, TradingStatusStreamRequest>)}`,
                mockResponse
            );
        });

        it('should handle tradingStatusStream() WebSocket stream data', () => {
            const configuration = new ConfigurationWebsocketStreams({});
            const websocketStreamClient = new WebsocketStreamsBase(configuration);
            const websocketStreamApi = new MarketStreamsApi(websocketStreamClient);

            const params: TradingStatusStreamRequest = {
                symbol: 'AAPL',
                id: 'e9d6b4349871b40611412680b3445fac',
            };

            const mockResponse = JSONParse(
                JSONStringify({
                    e: 'tradingStatus',
                    symbol: 'AAPL',
                    assetCode: 'EQ_AAPL',
                    status: 'TRADING_HALTED',
                    msg: 'MATERIAL_NEWS',
                    time: 1614021300000,
                    z: 'C',
                    tradability: 'NONE',
                })
            );

            const stream = websocketStreamApi.tradingStatusStream(params);
            const mockCallback = jest.fn(() => {});
            stream.on('message', mockCallback);

            websocketStreamClient['onMessage'](
                JSONStringify({
                    stream: replaceWebsocketStreamsPlaceholders(
                        '/<symbol>@tradingStatus'.slice(1),
                        params as unknown as Record<string, TradingStatusStreamRequest>
                    ),
                    data: mockResponse,
                }),
                websocketStreamClient.connectionPool[0]
            );

            expect(mockCallback).toHaveBeenCalledWith(mockResponse);
        });

        it('should throw RequiredError when symbol is missing', () => {
            const configuration = new ConfigurationWebsocketStreams({});
            const websocketStreamClient = new WebsocketStreamsBase(configuration);
            const websocketStreamApi = new MarketStreamsApi(websocketStreamClient);

            const _params: TradingStatusStreamRequest = {
                symbol: 'AAPL',
            };
            const params = Object.assign({ ..._params });
            delete params?.symbol;

            expect(() => websocketStreamApi.tradingStatusStream(params)).toThrow(
                'Required parameter symbol was null or undefined when calling tradingStatusStream.'
            );
        });
    });
});
