/**
 * Stocks Trading REST API
 *
 * REST APIs for Binance Stocks Trading. All endpoints under `/sapi/v1/equity/_*`.
 *
 * The version of the OpenAPI document: 1.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { jest, expect, beforeEach, describe, it } from '@jest/globals';
import { JSONParse, JSONStringify } from 'json-with-bigint';
import { ConfigurationRestAPI, type RestApiResponse } from '@binance/common';

import { TokenizedApi, TokenizedConvertStatusConvertTypeEnum } from '../../../src/rest-api';
import {
    TokenizedConvertHistoryRequest,
    TokenizedConvertStatusRequest,
    TokenizedMintRequest,
    TokenizedRedeemRequest,
} from '../../../src/rest-api';
import type {
    TokenizedConvertHistoryResponse,
    TokenizedConvertStatusResponse,
    TokenizedMintResponse,
    TokenizedRedeemResponse,
} from '../../../src/rest-api/types';

describe('TokenizedApi', () => {
    let client: TokenizedApi;
    let config: ConfigurationRestAPI;
    let mockResponse: object = {};

    beforeEach(() => {
        config = new ConfigurationRestAPI({
            apiKey: 'test-api-key',
            apiSecret: 'test-api-secret',
            basePath: '',
        });
        client = new TokenizedApi(config);
    });

    describe('tokenizedConvertHistory()', () => {
        it('should execute tokenizedConvertHistory() successfully with required parameters only', async () => {
            mockResponse = JSONParse(
                JSONStringify({
                    rows: [
                        {
                            underlyingAsset: 'AAPL',
                            underlyingAssetAmount: '1.000000000',
                            tokenizedAsset: 'AAPLB',
                            tokenizedAssetAmount: '1.000000000',
                            issuerRequestId: 'mint-20260505-8f3b9e1a2d3c4b5a',
                            convertType: 'MINT',
                            status: 'S',
                            createdAt: 1735900000000,
                            updatedAt: 1735900060000,
                        },
                    ],
                    hasMore: true,
                    nextLastId: 10020,
                })
            );

            const spy = jest.spyOn(client, 'tokenizedConvertHistory').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<TokenizedConvertHistoryResponse>)
            );
            const response = await client.tokenizedConvertHistory();
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should execute tokenizedConvertHistory() successfully with optional parameters', async () => {
            const params: TokenizedConvertHistoryRequest = {
                startTime: 1735800000000,
                endTime: 1735900000000,
                lastId: 10019,
                size: 20,
                recvWindow: 5000,
            };

            mockResponse = JSONParse(
                JSONStringify({
                    rows: [
                        {
                            underlyingAsset: 'AAPL',
                            underlyingAssetAmount: '1.000000000',
                            tokenizedAsset: 'AAPLB',
                            tokenizedAssetAmount: '1.000000000',
                            issuerRequestId: 'mint-20260505-8f3b9e1a2d3c4b5a',
                            convertType: 'MINT',
                            status: 'S',
                            createdAt: 1735900000000,
                            updatedAt: 1735900060000,
                        },
                    ],
                    hasMore: true,
                    nextLastId: 10020,
                })
            );

            const spy = jest.spyOn(client, 'tokenizedConvertHistory').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<TokenizedConvertHistoryResponse>)
            );
            const response = await client.tokenizedConvertHistory(params);
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should throw an error when server is returning an error', async () => {
            const errorResponse = {
                code: -1111,
                msg: 'Server Error',
            };

            const mockError = new Error('ResponseError') as Error & {
                response?: { status: number; data: unknown };
            };
            mockError.response = { status: 400, data: errorResponse };
            const spy = jest
                .spyOn(client, 'tokenizedConvertHistory')
                .mockRejectedValueOnce(mockError);
            await expect(client.tokenizedConvertHistory()).rejects.toThrow('ResponseError');
            spy.mockRestore();
        });
    });

    describe('tokenizedConvertStatus()', () => {
        it('should execute tokenizedConvertStatus() successfully with required parameters only', async () => {
            const params: TokenizedConvertStatusRequest = {
                issuerRequestId: 'mint-20260505-8f3b9e1a2d3c4b5a',
                convertType: TokenizedConvertStatusConvertTypeEnum.MINT,
            };

            mockResponse = JSONParse(
                JSONStringify({
                    underlyingAsset: 'AAPL',
                    underlyingAssetAmount: '1.000000000',
                    tokenizedAsset: 'AAPLB',
                    tokenizedAssetAmount: '1.000000000',
                    issuerRequestId: 'mint-20260505-8f3b9e1a2d3c4b5a',
                    convertType: 'MINT',
                    status: 'S',
                    createdAt: 1735900000000,
                    updatedAt: 1735900060000,
                })
            );

            const spy = jest.spyOn(client, 'tokenizedConvertStatus').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<TokenizedConvertStatusResponse>)
            );
            const response = await client.tokenizedConvertStatus(params);
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should execute tokenizedConvertStatus() successfully with optional parameters', async () => {
            const params: TokenizedConvertStatusRequest = {
                issuerRequestId: 'mint-20260505-8f3b9e1a2d3c4b5a',
                convertType: TokenizedConvertStatusConvertTypeEnum.MINT,
                recvWindow: 5000,
            };

            mockResponse = JSONParse(
                JSONStringify({
                    underlyingAsset: 'AAPL',
                    underlyingAssetAmount: '1.000000000',
                    tokenizedAsset: 'AAPLB',
                    tokenizedAssetAmount: '1.000000000',
                    issuerRequestId: 'mint-20260505-8f3b9e1a2d3c4b5a',
                    convertType: 'MINT',
                    status: 'S',
                    createdAt: 1735900000000,
                    updatedAt: 1735900060000,
                })
            );

            const spy = jest.spyOn(client, 'tokenizedConvertStatus').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<TokenizedConvertStatusResponse>)
            );
            const response = await client.tokenizedConvertStatus(params);
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should throw RequiredError when issuerRequestId is missing', async () => {
            const _params: TokenizedConvertStatusRequest = {
                issuerRequestId: 'mint-20260505-8f3b9e1a2d3c4b5a',
                convertType: TokenizedConvertStatusConvertTypeEnum.MINT,
            };
            const params = Object.assign({ ..._params });
            delete params?.issuerRequestId;

            await expect(client.tokenizedConvertStatus(params)).rejects.toThrow(
                'Required parameter issuerRequestId was null or undefined when calling tokenizedConvertStatus.'
            );
        });

        it('should throw RequiredError when convertType is missing', async () => {
            const _params: TokenizedConvertStatusRequest = {
                issuerRequestId: 'mint-20260505-8f3b9e1a2d3c4b5a',
                convertType: TokenizedConvertStatusConvertTypeEnum.MINT,
            };
            const params = Object.assign({ ..._params });
            delete params?.convertType;

            await expect(client.tokenizedConvertStatus(params)).rejects.toThrow(
                'Required parameter convertType was null or undefined when calling tokenizedConvertStatus.'
            );
        });

        it('should throw an error when server is returning an error', async () => {
            const params: TokenizedConvertStatusRequest = {
                issuerRequestId: 'mint-20260505-8f3b9e1a2d3c4b5a',
                convertType: TokenizedConvertStatusConvertTypeEnum.MINT,
            };

            const errorResponse = {
                code: -1111,
                msg: 'Server Error',
            };

            const mockError = new Error('ResponseError') as Error & {
                response?: { status: number; data: unknown };
            };
            mockError.response = { status: 400, data: errorResponse };
            const spy = jest
                .spyOn(client, 'tokenizedConvertStatus')
                .mockRejectedValueOnce(mockError);
            await expect(client.tokenizedConvertStatus(params)).rejects.toThrow('ResponseError');
            spy.mockRestore();
        });
    });

    describe('tokenizedMint()', () => {
        it('should execute tokenizedMint() successfully with required parameters only', async () => {
            const params: TokenizedMintRequest = {
                underlyingAsset: 'AAPL',
                underlyingAssetAmount: '1',
            };

            mockResponse = JSONParse(
                JSONStringify({ issuerRequestId: 'mint-20260505-8f3b9e1a2d3c4b5a', status: 'P' })
            );

            const spy = jest.spyOn(client, 'tokenizedMint').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<TokenizedMintResponse>)
            );
            const response = await client.tokenizedMint(params);
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should execute tokenizedMint() successfully with optional parameters', async () => {
            const params: TokenizedMintRequest = {
                underlyingAsset: 'AAPL',
                underlyingAssetAmount: '1',
                clientOrderId: 'mint-client-id-32chars-0000000001',
                recvWindow: 5000,
            };

            mockResponse = JSONParse(
                JSONStringify({ issuerRequestId: 'mint-20260505-8f3b9e1a2d3c4b5a', status: 'P' })
            );

            const spy = jest.spyOn(client, 'tokenizedMint').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<TokenizedMintResponse>)
            );
            const response = await client.tokenizedMint(params);
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should throw RequiredError when underlyingAsset is missing', async () => {
            const _params: TokenizedMintRequest = {
                underlyingAsset: 'AAPL',
                underlyingAssetAmount: '1',
            };
            const params = Object.assign({ ..._params });
            delete params?.underlyingAsset;

            await expect(client.tokenizedMint(params)).rejects.toThrow(
                'Required parameter underlyingAsset was null or undefined when calling tokenizedMint.'
            );
        });

        it('should throw RequiredError when underlyingAssetAmount is missing', async () => {
            const _params: TokenizedMintRequest = {
                underlyingAsset: 'AAPL',
                underlyingAssetAmount: '1',
            };
            const params = Object.assign({ ..._params });
            delete params?.underlyingAssetAmount;

            await expect(client.tokenizedMint(params)).rejects.toThrow(
                'Required parameter underlyingAssetAmount was null or undefined when calling tokenizedMint.'
            );
        });

        it('should throw an error when server is returning an error', async () => {
            const params: TokenizedMintRequest = {
                underlyingAsset: 'AAPL',
                underlyingAssetAmount: '1',
            };

            const errorResponse = {
                code: -1111,
                msg: 'Server Error',
            };

            const mockError = new Error('ResponseError') as Error & {
                response?: { status: number; data: unknown };
            };
            mockError.response = { status: 400, data: errorResponse };
            const spy = jest.spyOn(client, 'tokenizedMint').mockRejectedValueOnce(mockError);
            await expect(client.tokenizedMint(params)).rejects.toThrow('ResponseError');
            spy.mockRestore();
        });
    });

    describe('tokenizedRedeem()', () => {
        it('should execute tokenizedRedeem() successfully with required parameters only', async () => {
            const params: TokenizedRedeemRequest = {
                tokenizedAsset: 'AAPLB',
                tokenizedAssetAmount: '1',
            };

            mockResponse = JSONParse(
                JSONStringify({ issuerRequestId: 'redeem-20260505-a1b2c3d4e5f6', status: 'P' })
            );

            const spy = jest.spyOn(client, 'tokenizedRedeem').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<TokenizedRedeemResponse>)
            );
            const response = await client.tokenizedRedeem(params);
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should execute tokenizedRedeem() successfully with optional parameters', async () => {
            const params: TokenizedRedeemRequest = {
                tokenizedAsset: 'AAPLB',
                tokenizedAssetAmount: '1',
                clientOrderId: 'redeem-client-id-32chars-000000001',
                recvWindow: 5000,
            };

            mockResponse = JSONParse(
                JSONStringify({ issuerRequestId: 'redeem-20260505-a1b2c3d4e5f6', status: 'P' })
            );

            const spy = jest.spyOn(client, 'tokenizedRedeem').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<TokenizedRedeemResponse>)
            );
            const response = await client.tokenizedRedeem(params);
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should throw RequiredError when tokenizedAsset is missing', async () => {
            const _params: TokenizedRedeemRequest = {
                tokenizedAsset: 'AAPLB',
                tokenizedAssetAmount: '1',
            };
            const params = Object.assign({ ..._params });
            delete params?.tokenizedAsset;

            await expect(client.tokenizedRedeem(params)).rejects.toThrow(
                'Required parameter tokenizedAsset was null or undefined when calling tokenizedRedeem.'
            );
        });

        it('should throw RequiredError when tokenizedAssetAmount is missing', async () => {
            const _params: TokenizedRedeemRequest = {
                tokenizedAsset: 'AAPLB',
                tokenizedAssetAmount: '1',
            };
            const params = Object.assign({ ..._params });
            delete params?.tokenizedAssetAmount;

            await expect(client.tokenizedRedeem(params)).rejects.toThrow(
                'Required parameter tokenizedAssetAmount was null or undefined when calling tokenizedRedeem.'
            );
        });

        it('should throw an error when server is returning an error', async () => {
            const params: TokenizedRedeemRequest = {
                tokenizedAsset: 'AAPLB',
                tokenizedAssetAmount: '1',
            };

            const errorResponse = {
                code: -1111,
                msg: 'Server Error',
            };

            const mockError = new Error('ResponseError') as Error & {
                response?: { status: number; data: unknown };
            };
            mockError.response = { status: 400, data: errorResponse };
            const spy = jest.spyOn(client, 'tokenizedRedeem').mockRejectedValueOnce(mockError);
            await expect(client.tokenizedRedeem(params)).rejects.toThrow('ResponseError');
            spy.mockRestore();
        });
    });
});
