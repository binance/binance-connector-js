/**
 * Stocks Trading REST API
 *
 * REST APIs for Binance Stocks Trading. All endpoints under `/sapi/v1/equity/_*`.
 *
 * The version of the OpenAPI document: 1.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { jest, expect, beforeEach, describe, it } from '@jest/globals';
import { JSONParse, JSONStringify } from 'json-with-bigint';
import { ConfigurationRestAPI, type RestApiResponse } from '@binance/common';

import { MarketDataApi } from '../../../src/rest-api';
import { ExchangeInfoRequest, LatestQuoteRequest } from '../../../src/rest-api';
import type {
    ExchangeInfoResponse,
    LatestQuoteResponse,
    TokenizedAssetsResponse,
} from '../../../src/rest-api/types';

describe('MarketDataApi', () => {
    let client: MarketDataApi;
    let config: ConfigurationRestAPI;
    let mockResponse: object = {};

    beforeEach(() => {
        config = new ConfigurationRestAPI({
            apiKey: 'test-api-key',
            apiSecret: 'test-api-secret',
            basePath: '',
        });
        client = new MarketDataApi(config);
    });

    describe('exchangeInfo()', () => {
        it('should execute exchangeInfo() successfully with required parameters only', async () => {
            mockResponse = JSONParse(
                JSONStringify({
                    timezone: 'UTC',
                    symbols: [
                        {
                            symbol: 'AAPL',
                            tradability: 'BUY_SELL',
                            tradabilityUpdateTime: 1735900000000,
                            overnightSupported: true,
                            fractionable: true,
                            fractionableEh: false,
                            extendedSession: true,
                            maxNumOrders: 200,
                            stepSize: '0.000000001',
                            multiplierUp: '1.10',
                            multiplierDown: '0.90',
                            minQty: '0.000000001',
                            maxQty: '100000.00000000',
                            minNotional: '1.00',
                            maxNotional: '1000000.00',
                            listingTime: 1700000000000,
                            delistingTime: 1,
                        },
                    ],
                })
            );

            const spy = jest.spyOn(client, 'exchangeInfo').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<ExchangeInfoResponse>)
            );
            const response = await client.exchangeInfo();
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should execute exchangeInfo() successfully with optional parameters', async () => {
            const params: ExchangeInfoRequest = {
                symbol: 'AAPL',
            };

            mockResponse = JSONParse(
                JSONStringify({
                    timezone: 'UTC',
                    symbols: [
                        {
                            symbol: 'AAPL',
                            tradability: 'BUY_SELL',
                            tradabilityUpdateTime: 1735900000000,
                            overnightSupported: true,
                            fractionable: true,
                            fractionableEh: false,
                            extendedSession: true,
                            maxNumOrders: 200,
                            stepSize: '0.000000001',
                            multiplierUp: '1.10',
                            multiplierDown: '0.90',
                            minQty: '0.000000001',
                            maxQty: '100000.00000000',
                            minNotional: '1.00',
                            maxNotional: '1000000.00',
                            listingTime: 1700000000000,
                            delistingTime: 1,
                        },
                    ],
                })
            );

            const spy = jest.spyOn(client, 'exchangeInfo').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<ExchangeInfoResponse>)
            );
            const response = await client.exchangeInfo(params);
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should throw an error when server is returning an error', async () => {
            const errorResponse = {
                code: -1111,
                msg: 'Server Error',
            };

            const mockError = new Error('ResponseError') as Error & {
                response?: { status: number; data: unknown };
            };
            mockError.response = { status: 400, data: errorResponse };
            const spy = jest.spyOn(client, 'exchangeInfo').mockRejectedValueOnce(mockError);
            await expect(client.exchangeInfo()).rejects.toThrow('ResponseError');
            spy.mockRestore();
        });
    });

    describe('latestQuote()', () => {
        it('should execute latestQuote() successfully with required parameters only', async () => {
            const params: LatestQuoteRequest = {
                symbol: 'AAPL',
            };

            mockResponse = JSONParse(
                JSONStringify({
                    symbol: 'AAPL',
                    bidPrice: '180.50',
                    askPrice: '180.52',
                    bidSize: 100,
                    askSize: 200,
                })
            );

            const spy = jest.spyOn(client, 'latestQuote').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<LatestQuoteResponse>)
            );
            const response = await client.latestQuote(params);
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should execute latestQuote() successfully with optional parameters', async () => {
            const params: LatestQuoteRequest = {
                symbol: 'AAPL',
            };

            mockResponse = JSONParse(
                JSONStringify({
                    symbol: 'AAPL',
                    bidPrice: '180.50',
                    askPrice: '180.52',
                    bidSize: 100,
                    askSize: 200,
                })
            );

            const spy = jest.spyOn(client, 'latestQuote').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<LatestQuoteResponse>)
            );
            const response = await client.latestQuote(params);
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should throw RequiredError when symbol is missing', async () => {
            const _params: LatestQuoteRequest = {
                symbol: 'AAPL',
            };
            const params = Object.assign({ ..._params });
            delete params?.symbol;

            await expect(client.latestQuote(params)).rejects.toThrow(
                'Required parameter symbol was null or undefined when calling latestQuote.'
            );
        });

        it('should throw an error when server is returning an error', async () => {
            const params: LatestQuoteRequest = {
                symbol: 'AAPL',
            };

            const errorResponse = {
                code: -1111,
                msg: 'Server Error',
            };

            const mockError = new Error('ResponseError') as Error & {
                response?: { status: number; data: unknown };
            };
            mockError.response = { status: 400, data: errorResponse };
            const spy = jest.spyOn(client, 'latestQuote').mockRejectedValueOnce(mockError);
            await expect(client.latestQuote(params)).rejects.toThrow('ResponseError');
            spy.mockRestore();
        });
    });

    describe('tokenizedAssets()', () => {
        it('should execute tokenizedAssets() successfully with required parameters only', async () => {
            mockResponse = JSONParse(
                JSONStringify([
                    {
                        assetCode: 'AAPLB',
                        assetName: 'Apple Inc. Tokenized Stock',
                        underlyingEquitySymbol: 'AAPL',
                        multiplier: '1',
                        multiplierValid: true,
                    },
                ])
            );

            const spy = jest.spyOn(client, 'tokenizedAssets').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<TokenizedAssetsResponse>)
            );
            const response = await client.tokenizedAssets();
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should throw an error when server is returning an error', async () => {
            const errorResponse = {
                code: -1111,
                msg: 'Server Error',
            };

            const mockError = new Error('ResponseError') as Error & {
                response?: { status: number; data: unknown };
            };
            mockError.response = { status: 400, data: errorResponse };
            const spy = jest.spyOn(client, 'tokenizedAssets').mockRejectedValueOnce(mockError);
            await expect(client.tokenizedAssets()).rejects.toThrow('ResponseError');
            spy.mockRestore();
        });
    });
});
