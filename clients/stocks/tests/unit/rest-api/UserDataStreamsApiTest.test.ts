/**
 * Stocks Trading REST API
 *
 * REST APIs for Binance Stocks Trading. All endpoints under `/sapi/v1/equity/_*`.
 *
 * The version of the OpenAPI document: 1.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { jest, expect, beforeEach, describe, it } from '@jest/globals';
import { JSONParse, JSONStringify } from 'json-with-bigint';
import { ConfigurationRestAPI, type RestApiResponse } from '@binance/common';

import { UserDataStreamsApi } from '../../../src/rest-api';
import { CreateRenewListenKeyRequest } from '../../../src/rest-api';
import type { CreateRenewListenKeyResponse } from '../../../src/rest-api/types';

describe('UserDataStreamsApi', () => {
    let client: UserDataStreamsApi;
    let config: ConfigurationRestAPI;
    let mockResponse: object = {};

    beforeEach(() => {
        config = new ConfigurationRestAPI({
            apiKey: 'test-api-key',
            apiSecret: 'test-api-secret',
            basePath: '',
        });
        client = new UserDataStreamsApi(config);
    });

    describe('createRenewListenKey()', () => {
        it('should execute createRenewListenKey() successfully with required parameters only', async () => {
            mockResponse = JSONParse(
                JSONStringify({
                    listenKey: 'pqia91ma19a5s61cv6a81va65sdf19v8a65s1cv1zuz3ee1c5xz2ef6ad7',
                })
            );

            const spy = jest.spyOn(client, 'createRenewListenKey').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<CreateRenewListenKeyResponse>)
            );
            const response = await client.createRenewListenKey();
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should execute createRenewListenKey() successfully with optional parameters', async () => {
            const params: CreateRenewListenKeyRequest = {
                recvWindow: 5000,
            };

            mockResponse = JSONParse(
                JSONStringify({
                    listenKey: 'pqia91ma19a5s61cv6a81va65sdf19v8a65s1cv1zuz3ee1c5xz2ef6ad7',
                })
            );

            const spy = jest.spyOn(client, 'createRenewListenKey').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<CreateRenewListenKeyResponse>)
            );
            const response = await client.createRenewListenKey(params);
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should throw an error when server is returning an error', async () => {
            const errorResponse = {
                code: -1111,
                msg: 'Server Error',
            };

            const mockError = new Error('ResponseError') as Error & {
                response?: { status: number; data: unknown };
            };
            mockError.response = { status: 400, data: errorResponse };
            const spy = jest.spyOn(client, 'createRenewListenKey').mockRejectedValueOnce(mockError);
            await expect(client.createRenewListenKey()).rejects.toThrow('ResponseError');
            spy.mockRestore();
        });
    });
});
