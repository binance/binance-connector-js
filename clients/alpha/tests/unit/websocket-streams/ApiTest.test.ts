/**
 * Alpha WebSocket Market Streams
 *
 * Access Alpha market streams over WebSocket.
 *
 * The version of the OpenAPI document: 1.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { jest, expect, describe, it } from '@jest/globals';
import { JSONParse, JSONStringify } from 'json-with-bigint';
import {
    ConfigurationWebsocketStreams,
    WebsocketStreamsBase,
    replaceWebsocketStreamsPlaceholders,
} from '@binance/common';
import {
    AggregateTradeStreamRequest,
    AllBookTickerStreamRequest,
    AllMiniTickerStreamRequest,
    AllTickerStreamRequest,
    AllTokens24hTickerStreamRequest,
    BookTickerStreamRequest,
    ContractKlineStreamRequest,
    FullDepthStreamRequest,
    KlineStreamRequest,
    MiniTickerStreamRequest,
    PartialDepthStreamRequest,
    TickerStreamRequest,
    TradeStreamRequest,
} from '../../../src/websocket-streams';
import {
    Api,
    ContractKlineStreamIntervalEnum,
    FullDepthStreamIntervalEnum,
    KlineStreamIntervalEnum,
    PartialDepthStreamLevelsEnum,
    PartialDepthStreamIntervalEnum,
} from '../../../src/websocket-streams';
import { mockSubscription } from './utils';

describe('Api', () => {
    describe('aggregateTradeStream()', () => {
        it('should execute aggregateTradeStream() successfully', async () => {
            const params: AggregateTradeStreamRequest = {
                symbol: 'alpha_116usdt',
                id: '1',
            };

            const mockResponse = JSONParse(
                JSONStringify({
                    e: 'aggTrade',
                    E: 1771828569861,
                    T: 1771828569702,
                    a: 2684294,
                    f: 2684294,
                    l: 2684294,
                    m: false,
                    p: '0.08530000',
                    q: '879.24000000',
                    s: 'ALPHA_474USDT',
                })
            );

            mockSubscription(
                `ws/${replaceWebsocketStreamsPlaceholders('/<symbol>@aggTrade'.slice(1), params as unknown as Record<string, AggregateTradeStreamRequest>)}`,
                mockResponse
            );
        });

        it('should handle aggregateTradeStream() WebSocket stream data', () => {
            const configuration = new ConfigurationWebsocketStreams({});
            const websocketStreamClient = new WebsocketStreamsBase(configuration);
            const websocketStreamApi = new Api(websocketStreamClient);

            const params: AggregateTradeStreamRequest = {
                symbol: 'alpha_116usdt',
                id: '1',
            };

            const mockResponse = JSONParse(
                JSONStringify({
                    e: 'aggTrade',
                    E: 1771828569861,
                    T: 1771828569702,
                    a: 2684294,
                    f: 2684294,
                    l: 2684294,
                    m: false,
                    p: '0.08530000',
                    q: '879.24000000',
                    s: 'ALPHA_474USDT',
                })
            );

            const stream = websocketStreamApi.aggregateTradeStream(params);
            const mockCallback = jest.fn(() => {});
            stream.on('message', mockCallback);

            websocketStreamClient['onMessage'](
                JSONStringify({
                    stream: replaceWebsocketStreamsPlaceholders(
                        '/<symbol>@aggTrade'.slice(1),
                        params as unknown as Record<string, AggregateTradeStreamRequest>
                    ),
                    data: mockResponse,
                }),
                websocketStreamClient.connectionPool[0]
            );

            expect(mockCallback).toHaveBeenCalledWith(mockResponse);
        });

        it('should throw RequiredError when symbol is missing', () => {
            const configuration = new ConfigurationWebsocketStreams({});
            const websocketStreamClient = new WebsocketStreamsBase(configuration);
            const websocketStreamApi = new Api(websocketStreamClient);

            const _params: AggregateTradeStreamRequest = {
                symbol: 'alpha_116usdt',
            };
            const params = Object.assign({ ..._params });
            delete params?.symbol;

            expect(() => websocketStreamApi.aggregateTradeStream(params)).toThrow(
                'Required parameter symbol was null or undefined when calling aggregateTradeStream.'
            );
        });
    });

    describe('allBookTickerStream()', () => {
        it('should execute allBookTickerStream() successfully', async () => {
            const params: AllBookTickerStreamRequest = {
                id: '1',
            };

            const mockResponse = JSONParse(
                JSONStringify({
                    e: 'bookTicker',
                    E: 1773108379067,
                    T: 1773108379054,
                    u: 6663207693,
                    s: 'ALPHA_116USDT',
                    b: '8.30000000',
                    B: '0.91076900',
                    a: '9.30000000',
                    A: '2.00353200',
                })
            );

            mockSubscription(
                `ws/${replaceWebsocketStreamsPlaceholders('/!bookTicker'.slice(1), params as unknown as Record<string, AllBookTickerStreamRequest>)}`,
                mockResponse
            );
        });

        it('should handle allBookTickerStream() WebSocket stream data', () => {
            const configuration = new ConfigurationWebsocketStreams({});
            const websocketStreamClient = new WebsocketStreamsBase(configuration);
            const websocketStreamApi = new Api(websocketStreamClient);

            const params: AllBookTickerStreamRequest = {
                id: '1',
            };

            const mockResponse = JSONParse(
                JSONStringify({
                    e: 'bookTicker',
                    E: 1773108379067,
                    T: 1773108379054,
                    u: 6663207693,
                    s: 'ALPHA_116USDT',
                    b: '8.30000000',
                    B: '0.91076900',
                    a: '9.30000000',
                    A: '2.00353200',
                })
            );

            const stream = websocketStreamApi.allBookTickerStream(params);
            const mockCallback = jest.fn(() => {});
            stream.on('message', mockCallback);

            websocketStreamClient['onMessage'](
                JSONStringify({
                    stream: replaceWebsocketStreamsPlaceholders(
                        '/!bookTicker'.slice(1),
                        params as unknown as Record<string, AllBookTickerStreamRequest>
                    ),
                    data: mockResponse,
                }),
                websocketStreamClient.connectionPool[0]
            );

            expect(mockCallback).toHaveBeenCalledWith(mockResponse);
        });
    });

    describe('allMiniTickerStream()', () => {
        it('should execute allMiniTickerStream() successfully', async () => {
            const params: AllMiniTickerStreamRequest = {
                id: '1',
            };

            const mockResponse = JSONParse(
                JSONStringify({
                    e: '24hrMiniTicker',
                    E: 1773109449908,
                    s: 'ALPHA_116USDT',
                    c: '8.40000000',
                    o: '8.40000000',
                    h: '8.50000000',
                    l: '8.30000000',
                    v: '64739.03978900',
                    q: '543810.36005090',
                })
            );

            mockSubscription(
                `ws/${replaceWebsocketStreamsPlaceholders('/!miniTicker@arr'.slice(1), params as unknown as Record<string, AllMiniTickerStreamRequest>)}`,
                mockResponse
            );
        });

        it('should handle allMiniTickerStream() WebSocket stream data', () => {
            const configuration = new ConfigurationWebsocketStreams({});
            const websocketStreamClient = new WebsocketStreamsBase(configuration);
            const websocketStreamApi = new Api(websocketStreamClient);

            const params: AllMiniTickerStreamRequest = {
                id: '1',
            };

            const mockResponse = JSONParse(
                JSONStringify({
                    e: '24hrMiniTicker',
                    E: 1773109449908,
                    s: 'ALPHA_116USDT',
                    c: '8.40000000',
                    o: '8.40000000',
                    h: '8.50000000',
                    l: '8.30000000',
                    v: '64739.03978900',
                    q: '543810.36005090',
                })
            );

            const stream = websocketStreamApi.allMiniTickerStream(params);
            const mockCallback = jest.fn(() => {});
            stream.on('message', mockCallback);

            websocketStreamClient['onMessage'](
                JSONStringify({
                    stream: replaceWebsocketStreamsPlaceholders(
                        '/!miniTicker@arr'.slice(1),
                        params as unknown as Record<string, AllMiniTickerStreamRequest>
                    ),
                    data: mockResponse,
                }),
                websocketStreamClient.connectionPool[0]
            );

            expect(mockCallback).toHaveBeenCalledWith(mockResponse);
        });
    });

    describe('allTickerStream()', () => {
        it('should execute allTickerStream() successfully', async () => {
            const params: AllTickerStreamRequest = {
                id: '1',
            };

            const mockResponse = JSONParse(
                JSONStringify({
                    e: '24hrTicker',
                    E: 1773109631569,
                    s: 'ALPHA_116USDT',
                    p: '0.00000000',
                    P: '0.00',
                    w: '8.40003127',
                    c: '8.40000000',
                    Q: '0.49293200',
                    o: '8.40000000',
                    h: '8.50000000',
                    l: '8.30000000',
                    v: '64750.63418500',
                    q: '543907.35169250',
                    O: 1773023220000,
                    C: 1773109631555,
                    F: 19847634,
                    L: 19911287,
                    n: 217505,
                })
            );

            mockSubscription(
                `ws/${replaceWebsocketStreamsPlaceholders('/!ticker@arr'.slice(1), params as unknown as Record<string, AllTickerStreamRequest>)}`,
                mockResponse
            );
        });

        it('should handle allTickerStream() WebSocket stream data', () => {
            const configuration = new ConfigurationWebsocketStreams({});
            const websocketStreamClient = new WebsocketStreamsBase(configuration);
            const websocketStreamApi = new Api(websocketStreamClient);

            const params: AllTickerStreamRequest = {
                id: '1',
            };

            const mockResponse = JSONParse(
                JSONStringify({
                    e: '24hrTicker',
                    E: 1773109631569,
                    s: 'ALPHA_116USDT',
                    p: '0.00000000',
                    P: '0.00',
                    w: '8.40003127',
                    c: '8.40000000',
                    Q: '0.49293200',
                    o: '8.40000000',
                    h: '8.50000000',
                    l: '8.30000000',
                    v: '64750.63418500',
                    q: '543907.35169250',
                    O: 1773023220000,
                    C: 1773109631555,
                    F: 19847634,
                    L: 19911287,
                    n: 217505,
                })
            );

            const stream = websocketStreamApi.allTickerStream(params);
            const mockCallback = jest.fn(() => {});
            stream.on('message', mockCallback);

            websocketStreamClient['onMessage'](
                JSONStringify({
                    stream: replaceWebsocketStreamsPlaceholders(
                        '/!ticker@arr'.slice(1),
                        params as unknown as Record<string, AllTickerStreamRequest>
                    ),
                    data: mockResponse,
                }),
                websocketStreamClient.connectionPool[0]
            );

            expect(mockCallback).toHaveBeenCalledWith(mockResponse);
        });
    });

    describe('allTokens24hTickerStream()', () => {
        it('should execute allTokens24hTickerStream() successfully', async () => {
            const params: AllTokens24hTickerStreamRequest = {
                id: '1',
            };

            const mockResponse = JSONParse(
                JSONStringify({
                    e: 'tickerList',
                    d: [
                        {
                            ca: '0x8fce...@56',
                            cnt24: 10426,
                            fdv: '72834145.18711072',
                            hc: '6822',
                            liq: '1442996.98151177662835',
                            mc: '13542034.24008014',
                            p: '0.072849536538593663',
                            pc24: '-2.32',
                            s: '1',
                            t: 1771825733000,
                            vol24: '12930712.213702157742688594285',
                        },
                    ],
                })
            );

            mockSubscription(
                `ws/${replaceWebsocketStreamsPlaceholders('/came@allTokens@ticker24'.slice(1), params as unknown as Record<string, AllTokens24hTickerStreamRequest>)}`,
                mockResponse
            );
        });

        it('should handle allTokens24hTickerStream() WebSocket stream data', () => {
            const configuration = new ConfigurationWebsocketStreams({});
            const websocketStreamClient = new WebsocketStreamsBase(configuration);
            const websocketStreamApi = new Api(websocketStreamClient);

            const params: AllTokens24hTickerStreamRequest = {
                id: '1',
            };

            const mockResponse = JSONParse(
                JSONStringify({
                    e: 'tickerList',
                    d: [
                        {
                            ca: '0x8fce...@56',
                            cnt24: 10426,
                            fdv: '72834145.18711072',
                            hc: '6822',
                            liq: '1442996.98151177662835',
                            mc: '13542034.24008014',
                            p: '0.072849536538593663',
                            pc24: '-2.32',
                            s: '1',
                            t: 1771825733000,
                            vol24: '12930712.213702157742688594285',
                        },
                    ],
                })
            );

            const stream = websocketStreamApi.allTokens24hTickerStream(params);
            const mockCallback = jest.fn(() => {});
            stream.on('message', mockCallback);

            websocketStreamClient['onMessage'](
                JSONStringify({
                    stream: replaceWebsocketStreamsPlaceholders(
                        '/came@allTokens@ticker24'.slice(1),
                        params as unknown as Record<string, AllTokens24hTickerStreamRequest>
                    ),
                    data: mockResponse,
                }),
                websocketStreamClient.connectionPool[0]
            );

            expect(mockCallback).toHaveBeenCalledWith(mockResponse);
        });
    });

    describe('bookTickerStream()', () => {
        it('should execute bookTickerStream() successfully', async () => {
            const params: BookTickerStreamRequest = {
                symbol: 'alpha_116usdt',
                id: '1',
            };

            const mockResponse = JSONParse(
                JSONStringify({
                    e: 'bookTicker',
                    E: 1773108379067,
                    T: 1773108379054,
                    u: 6663207693,
                    s: 'ALPHA_116USDT',
                    b: '8.30000000',
                    B: '0.91076900',
                    a: '9.30000000',
                    A: '2.00353200',
                })
            );

            mockSubscription(
                `ws/${replaceWebsocketStreamsPlaceholders('/<symbol>@bookTicker'.slice(1), params as unknown as Record<string, BookTickerStreamRequest>)}`,
                mockResponse
            );
        });

        it('should handle bookTickerStream() WebSocket stream data', () => {
            const configuration = new ConfigurationWebsocketStreams({});
            const websocketStreamClient = new WebsocketStreamsBase(configuration);
            const websocketStreamApi = new Api(websocketStreamClient);

            const params: BookTickerStreamRequest = {
                symbol: 'alpha_116usdt',
                id: '1',
            };

            const mockResponse = JSONParse(
                JSONStringify({
                    e: 'bookTicker',
                    E: 1773108379067,
                    T: 1773108379054,
                    u: 6663207693,
                    s: 'ALPHA_116USDT',
                    b: '8.30000000',
                    B: '0.91076900',
                    a: '9.30000000',
                    A: '2.00353200',
                })
            );

            const stream = websocketStreamApi.bookTickerStream(params);
            const mockCallback = jest.fn(() => {});
            stream.on('message', mockCallback);

            websocketStreamClient['onMessage'](
                JSONStringify({
                    stream: replaceWebsocketStreamsPlaceholders(
                        '/<symbol>@bookTicker'.slice(1),
                        params as unknown as Record<string, BookTickerStreamRequest>
                    ),
                    data: mockResponse,
                }),
                websocketStreamClient.connectionPool[0]
            );

            expect(mockCallback).toHaveBeenCalledWith(mockResponse);
        });

        it('should throw RequiredError when symbol is missing', () => {
            const configuration = new ConfigurationWebsocketStreams({});
            const websocketStreamClient = new WebsocketStreamsBase(configuration);
            const websocketStreamApi = new Api(websocketStreamClient);

            const _params: BookTickerStreamRequest = {
                symbol: 'alpha_116usdt',
            };
            const params = Object.assign({ ..._params });
            delete params?.symbol;

            expect(() => websocketStreamApi.bookTickerStream(params)).toThrow(
                'Required parameter symbol was null or undefined when calling bookTickerStream.'
            );
        });
    });

    describe('contractKlineStream()', () => {
        it('should execute contractKlineStream() successfully', async () => {
            const params: ContractKlineStreamRequest = {
                contractAddress: 'G7vQWurMkMMm2dU3iZpXYFTHT9Biio4F4gZCrwFpKNwG',
                chainId: 'CT_501',
                interval: ContractKlineStreamIntervalEnum.INTERVAL_1s,
                id: '1',
            };

            const mockResponse = JSONParse(
                JSONStringify({
                    ca: 'G7vQW...@CT_501',
                    e: 'kline',
                    k: {
                        o: '0.15584372258086979622',
                        c: '0.15584957424118055058',
                        h: '0.15584957424118055058',
                        l: '0.15584372258086979622',
                        v: '2.8584092999332775480',
                        ot: 1771828655000,
                        ct: 1771828656000,
                        i: '1s',
                    },
                })
            );

            mockSubscription(
                `ws/${replaceWebsocketStreamsPlaceholders('/came@<contractAddress>@<chainId>@kline_<interval>'.slice(1), params as unknown as Record<string, ContractKlineStreamRequest>)}`,
                mockResponse
            );
        });

        it('should handle contractKlineStream() WebSocket stream data', () => {
            const configuration = new ConfigurationWebsocketStreams({});
            const websocketStreamClient = new WebsocketStreamsBase(configuration);
            const websocketStreamApi = new Api(websocketStreamClient);

            const params: ContractKlineStreamRequest = {
                contractAddress: 'G7vQWurMkMMm2dU3iZpXYFTHT9Biio4F4gZCrwFpKNwG',
                chainId: 'CT_501',
                interval: ContractKlineStreamIntervalEnum.INTERVAL_1s,
                id: '1',
            };

            const mockResponse = JSONParse(
                JSONStringify({
                    ca: 'G7vQW...@CT_501',
                    e: 'kline',
                    k: {
                        o: '0.15584372258086979622',
                        c: '0.15584957424118055058',
                        h: '0.15584957424118055058',
                        l: '0.15584372258086979622',
                        v: '2.8584092999332775480',
                        ot: 1771828655000,
                        ct: 1771828656000,
                        i: '1s',
                    },
                })
            );

            const stream = websocketStreamApi.contractKlineStream(params);
            const mockCallback = jest.fn(() => {});
            stream.on('message', mockCallback);

            websocketStreamClient['onMessage'](
                JSONStringify({
                    stream: replaceWebsocketStreamsPlaceholders(
                        '/came@<contractAddress>@<chainId>@kline_<interval>'.slice(1),
                        params as unknown as Record<string, ContractKlineStreamRequest>
                    ),
                    data: mockResponse,
                }),
                websocketStreamClient.connectionPool[0]
            );

            expect(mockCallback).toHaveBeenCalledWith(mockResponse);
        });

        it('should throw RequiredError when contractAddress is missing', () => {
            const configuration = new ConfigurationWebsocketStreams({});
            const websocketStreamClient = new WebsocketStreamsBase(configuration);
            const websocketStreamApi = new Api(websocketStreamClient);

            const _params: ContractKlineStreamRequest = {
                contractAddress: 'G7vQWurMkMMm2dU3iZpXYFTHT9Biio4F4gZCrwFpKNwG',
                chainId: 'CT_501',
                interval: ContractKlineStreamIntervalEnum.INTERVAL_1s,
            };
            const params = Object.assign({ ..._params });
            delete params?.contractAddress;

            expect(() => websocketStreamApi.contractKlineStream(params)).toThrow(
                'Required parameter contractAddress was null or undefined when calling contractKlineStream.'
            );
        });

        it('should throw RequiredError when chainId is missing', () => {
            const configuration = new ConfigurationWebsocketStreams({});
            const websocketStreamClient = new WebsocketStreamsBase(configuration);
            const websocketStreamApi = new Api(websocketStreamClient);

            const _params: ContractKlineStreamRequest = {
                contractAddress: 'G7vQWurMkMMm2dU3iZpXYFTHT9Biio4F4gZCrwFpKNwG',
                chainId: 'CT_501',
                interval: ContractKlineStreamIntervalEnum.INTERVAL_1s,
            };
            const params = Object.assign({ ..._params });
            delete params?.chainId;

            expect(() => websocketStreamApi.contractKlineStream(params)).toThrow(
                'Required parameter chainId was null or undefined when calling contractKlineStream.'
            );
        });

        it('should throw RequiredError when interval is missing', () => {
            const configuration = new ConfigurationWebsocketStreams({});
            const websocketStreamClient = new WebsocketStreamsBase(configuration);
            const websocketStreamApi = new Api(websocketStreamClient);

            const _params: ContractKlineStreamRequest = {
                contractAddress: 'G7vQWurMkMMm2dU3iZpXYFTHT9Biio4F4gZCrwFpKNwG',
                chainId: 'CT_501',
                interval: ContractKlineStreamIntervalEnum.INTERVAL_1s,
            };
            const params = Object.assign({ ..._params });
            delete params?.interval;

            expect(() => websocketStreamApi.contractKlineStream(params)).toThrow(
                'Required parameter interval was null or undefined when calling contractKlineStream.'
            );
        });
    });

    describe('fullDepthStream()', () => {
        it('should execute fullDepthStream() successfully', async () => {
            const params: FullDepthStreamRequest = {
                symbol: 'alpha_116usdt',
                interval: FullDepthStreamIntervalEnum.INTERVAL_0ms,
                id: '1',
            };

            const mockResponse = JSONParse(
                JSONStringify({
                    e: 'depthUpdate',
                    E: 1771828614381,
                    T: 1771828614258,
                    U: 42648947917,
                    u: 42648948229,
                    pu: 42648947321,
                    s: 'ALPHA_474USDT',
                    b: [
                        ['0.16479999', '0'],
                        ['0.16480000', '18.68000000'],
                    ],
                    a: [
                        ['0.16490000', '500.00000000'],
                        ['0.16500000', '0'],
                    ],
                })
            );

            mockSubscription(
                `ws/${replaceWebsocketStreamsPlaceholders('/<symbol>@fulldepth@<interval>'.slice(1), params as unknown as Record<string, FullDepthStreamRequest>)}`,
                mockResponse
            );
        });

        it('should handle fullDepthStream() WebSocket stream data', () => {
            const configuration = new ConfigurationWebsocketStreams({});
            const websocketStreamClient = new WebsocketStreamsBase(configuration);
            const websocketStreamApi = new Api(websocketStreamClient);

            const params: FullDepthStreamRequest = {
                symbol: 'alpha_116usdt',
                interval: FullDepthStreamIntervalEnum.INTERVAL_0ms,
                id: '1',
            };

            const mockResponse = JSONParse(
                JSONStringify({
                    e: 'depthUpdate',
                    E: 1771828614381,
                    T: 1771828614258,
                    U: 42648947917,
                    u: 42648948229,
                    pu: 42648947321,
                    s: 'ALPHA_474USDT',
                    b: [
                        ['0.16479999', '0'],
                        ['0.16480000', '18.68000000'],
                    ],
                    a: [
                        ['0.16490000', '500.00000000'],
                        ['0.16500000', '0'],
                    ],
                })
            );

            const stream = websocketStreamApi.fullDepthStream(params);
            const mockCallback = jest.fn(() => {});
            stream.on('message', mockCallback);

            websocketStreamClient['onMessage'](
                JSONStringify({
                    stream: replaceWebsocketStreamsPlaceholders(
                        '/<symbol>@fulldepth@<interval>'.slice(1),
                        params as unknown as Record<string, FullDepthStreamRequest>
                    ),
                    data: mockResponse,
                }),
                websocketStreamClient.connectionPool[0]
            );

            expect(mockCallback).toHaveBeenCalledWith(mockResponse);
        });

        it('should throw RequiredError when symbol is missing', () => {
            const configuration = new ConfigurationWebsocketStreams({});
            const websocketStreamClient = new WebsocketStreamsBase(configuration);
            const websocketStreamApi = new Api(websocketStreamClient);

            const _params: FullDepthStreamRequest = {
                symbol: 'alpha_116usdt',
                interval: FullDepthStreamIntervalEnum.INTERVAL_0ms,
            };
            const params = Object.assign({ ..._params });
            delete params?.symbol;

            expect(() => websocketStreamApi.fullDepthStream(params)).toThrow(
                'Required parameter symbol was null or undefined when calling fullDepthStream.'
            );
        });

        it('should throw RequiredError when interval is missing', () => {
            const configuration = new ConfigurationWebsocketStreams({});
            const websocketStreamClient = new WebsocketStreamsBase(configuration);
            const websocketStreamApi = new Api(websocketStreamClient);

            const _params: FullDepthStreamRequest = {
                symbol: 'alpha_116usdt',
                interval: FullDepthStreamIntervalEnum.INTERVAL_0ms,
            };
            const params = Object.assign({ ..._params });
            delete params?.interval;

            expect(() => websocketStreamApi.fullDepthStream(params)).toThrow(
                'Required parameter interval was null or undefined when calling fullDepthStream.'
            );
        });
    });

    describe('klineStream()', () => {
        it('should execute klineStream() successfully', async () => {
            const params: KlineStreamRequest = {
                symbol: 'alpha_116usdt',
                interval: KlineStreamIntervalEnum.INTERVAL_1m,
                id: '1',
            };

            const mockResponse = JSONParse(
                JSONStringify({
                    e: 'kline',
                    E: 1773111055144,
                    s: 'ALPHA_116USDT',
                    k: {
                        t: 1773111000000,
                        T: 1773111059999,
                        s: 'ALPHA_116USDT',
                        i: '1m',
                        f: 19912696,
                        L: 19912765,
                        o: '8.40000000',
                        c: '8.40000000',
                        h: '8.40000000',
                        l: '8.40000000',
                        v: '63.64876300',
                        n: 218,
                        x: false,
                        q: '534.64960920',
                        V: '63.64876300',
                        Q: '534.64960920',
                        B: '0',
                    },
                })
            );

            mockSubscription(
                `ws/${replaceWebsocketStreamsPlaceholders('/<symbol>@kline_<interval>'.slice(1), params as unknown as Record<string, KlineStreamRequest>)}`,
                mockResponse
            );
        });

        it('should handle klineStream() WebSocket stream data', () => {
            const configuration = new ConfigurationWebsocketStreams({});
            const websocketStreamClient = new WebsocketStreamsBase(configuration);
            const websocketStreamApi = new Api(websocketStreamClient);

            const params: KlineStreamRequest = {
                symbol: 'alpha_116usdt',
                interval: KlineStreamIntervalEnum.INTERVAL_1m,
                id: '1',
            };

            const mockResponse = JSONParse(
                JSONStringify({
                    e: 'kline',
                    E: 1773111055144,
                    s: 'ALPHA_116USDT',
                    k: {
                        t: 1773111000000,
                        T: 1773111059999,
                        s: 'ALPHA_116USDT',
                        i: '1m',
                        f: 19912696,
                        L: 19912765,
                        o: '8.40000000',
                        c: '8.40000000',
                        h: '8.40000000',
                        l: '8.40000000',
                        v: '63.64876300',
                        n: 218,
                        x: false,
                        q: '534.64960920',
                        V: '63.64876300',
                        Q: '534.64960920',
                        B: '0',
                    },
                })
            );

            const stream = websocketStreamApi.klineStream(params);
            const mockCallback = jest.fn(() => {});
            stream.on('message', mockCallback);

            websocketStreamClient['onMessage'](
                JSONStringify({
                    stream: replaceWebsocketStreamsPlaceholders(
                        '/<symbol>@kline_<interval>'.slice(1),
                        params as unknown as Record<string, KlineStreamRequest>
                    ),
                    data: mockResponse,
                }),
                websocketStreamClient.connectionPool[0]
            );

            expect(mockCallback).toHaveBeenCalledWith(mockResponse);
        });

        it('should throw RequiredError when symbol is missing', () => {
            const configuration = new ConfigurationWebsocketStreams({});
            const websocketStreamClient = new WebsocketStreamsBase(configuration);
            const websocketStreamApi = new Api(websocketStreamClient);

            const _params: KlineStreamRequest = {
                symbol: 'alpha_116usdt',
                interval: KlineStreamIntervalEnum.INTERVAL_1m,
            };
            const params = Object.assign({ ..._params });
            delete params?.symbol;

            expect(() => websocketStreamApi.klineStream(params)).toThrow(
                'Required parameter symbol was null or undefined when calling klineStream.'
            );
        });

        it('should throw RequiredError when interval is missing', () => {
            const configuration = new ConfigurationWebsocketStreams({});
            const websocketStreamClient = new WebsocketStreamsBase(configuration);
            const websocketStreamApi = new Api(websocketStreamClient);

            const _params: KlineStreamRequest = {
                symbol: 'alpha_116usdt',
                interval: KlineStreamIntervalEnum.INTERVAL_1m,
            };
            const params = Object.assign({ ..._params });
            delete params?.interval;

            expect(() => websocketStreamApi.klineStream(params)).toThrow(
                'Required parameter interval was null or undefined when calling klineStream.'
            );
        });
    });

    describe('miniTickerStream()', () => {
        it('should execute miniTickerStream() successfully', async () => {
            const params: MiniTickerStreamRequest = {
                symbol: 'alpha_116usdt',
                id: '1',
            };

            const mockResponse = JSONParse(
                JSONStringify({
                    e: '24hrMiniTicker',
                    E: 1773109449908,
                    s: 'ALPHA_116USDT',
                    c: '8.40000000',
                    o: '8.40000000',
                    h: '8.50000000',
                    l: '8.30000000',
                    v: '64739.03978900',
                    q: '543810.36005090',
                })
            );

            mockSubscription(
                `ws/${replaceWebsocketStreamsPlaceholders('/<symbol>@miniTicker'.slice(1), params as unknown as Record<string, MiniTickerStreamRequest>)}`,
                mockResponse
            );
        });

        it('should handle miniTickerStream() WebSocket stream data', () => {
            const configuration = new ConfigurationWebsocketStreams({});
            const websocketStreamClient = new WebsocketStreamsBase(configuration);
            const websocketStreamApi = new Api(websocketStreamClient);

            const params: MiniTickerStreamRequest = {
                symbol: 'alpha_116usdt',
                id: '1',
            };

            const mockResponse = JSONParse(
                JSONStringify({
                    e: '24hrMiniTicker',
                    E: 1773109449908,
                    s: 'ALPHA_116USDT',
                    c: '8.40000000',
                    o: '8.40000000',
                    h: '8.50000000',
                    l: '8.30000000',
                    v: '64739.03978900',
                    q: '543810.36005090',
                })
            );

            const stream = websocketStreamApi.miniTickerStream(params);
            const mockCallback = jest.fn(() => {});
            stream.on('message', mockCallback);

            websocketStreamClient['onMessage'](
                JSONStringify({
                    stream: replaceWebsocketStreamsPlaceholders(
                        '/<symbol>@miniTicker'.slice(1),
                        params as unknown as Record<string, MiniTickerStreamRequest>
                    ),
                    data: mockResponse,
                }),
                websocketStreamClient.connectionPool[0]
            );

            expect(mockCallback).toHaveBeenCalledWith(mockResponse);
        });

        it('should throw RequiredError when symbol is missing', () => {
            const configuration = new ConfigurationWebsocketStreams({});
            const websocketStreamClient = new WebsocketStreamsBase(configuration);
            const websocketStreamApi = new Api(websocketStreamClient);

            const _params: MiniTickerStreamRequest = {
                symbol: 'alpha_116usdt',
            };
            const params = Object.assign({ ..._params });
            delete params?.symbol;

            expect(() => websocketStreamApi.miniTickerStream(params)).toThrow(
                'Required parameter symbol was null or undefined when calling miniTickerStream.'
            );
        });
    });

    describe('partialDepthStream()', () => {
        it('should execute partialDepthStream() successfully', async () => {
            const params: PartialDepthStreamRequest = {
                symbol: 'alpha_116usdt',
                levels: PartialDepthStreamLevelsEnum.LEVELS_5,
                interval: PartialDepthStreamIntervalEnum.INTERVAL_0ms,
                id: '1',
            };

            const mockResponse = JSONParse(
                JSONStringify({
                    e: 'depthUpdate',
                    E: 1773110222878,
                    T: 1773110222767,
                    U: 6663367135,
                    u: 6663367147,
                    pu: 6663366971,
                    s: 'ALPHA_116USDT',
                    b: [
                        ['0.16479999', '0'],
                        ['0.16480000', '18.68000000'],
                    ],
                    a: [
                        ['0.16490000', '500.00000000'],
                        ['0.16500000', '0'],
                    ],
                })
            );

            mockSubscription(
                `ws/${replaceWebsocketStreamsPlaceholders('/<symbol>@depth<levels>@<interval>'.slice(1), params as unknown as Record<string, PartialDepthStreamRequest>)}`,
                mockResponse
            );
        });

        it('should handle partialDepthStream() WebSocket stream data', () => {
            const configuration = new ConfigurationWebsocketStreams({});
            const websocketStreamClient = new WebsocketStreamsBase(configuration);
            const websocketStreamApi = new Api(websocketStreamClient);

            const params: PartialDepthStreamRequest = {
                symbol: 'alpha_116usdt',
                levels: PartialDepthStreamLevelsEnum.LEVELS_5,
                interval: PartialDepthStreamIntervalEnum.INTERVAL_0ms,
                id: '1',
            };

            const mockResponse = JSONParse(
                JSONStringify({
                    e: 'depthUpdate',
                    E: 1773110222878,
                    T: 1773110222767,
                    U: 6663367135,
                    u: 6663367147,
                    pu: 6663366971,
                    s: 'ALPHA_116USDT',
                    b: [
                        ['0.16479999', '0'],
                        ['0.16480000', '18.68000000'],
                    ],
                    a: [
                        ['0.16490000', '500.00000000'],
                        ['0.16500000', '0'],
                    ],
                })
            );

            const stream = websocketStreamApi.partialDepthStream(params);
            const mockCallback = jest.fn(() => {});
            stream.on('message', mockCallback);

            websocketStreamClient['onMessage'](
                JSONStringify({
                    stream: replaceWebsocketStreamsPlaceholders(
                        '/<symbol>@depth<levels>@<interval>'.slice(1),
                        params as unknown as Record<string, PartialDepthStreamRequest>
                    ),
                    data: mockResponse,
                }),
                websocketStreamClient.connectionPool[0]
            );

            expect(mockCallback).toHaveBeenCalledWith(mockResponse);
        });

        it('should throw RequiredError when symbol is missing', () => {
            const configuration = new ConfigurationWebsocketStreams({});
            const websocketStreamClient = new WebsocketStreamsBase(configuration);
            const websocketStreamApi = new Api(websocketStreamClient);

            const _params: PartialDepthStreamRequest = {
                symbol: 'alpha_116usdt',
                levels: PartialDepthStreamLevelsEnum.LEVELS_5,
                interval: PartialDepthStreamIntervalEnum.INTERVAL_0ms,
            };
            const params = Object.assign({ ..._params });
            delete params?.symbol;

            expect(() => websocketStreamApi.partialDepthStream(params)).toThrow(
                'Required parameter symbol was null or undefined when calling partialDepthStream.'
            );
        });

        it('should throw RequiredError when levels is missing', () => {
            const configuration = new ConfigurationWebsocketStreams({});
            const websocketStreamClient = new WebsocketStreamsBase(configuration);
            const websocketStreamApi = new Api(websocketStreamClient);

            const _params: PartialDepthStreamRequest = {
                symbol: 'alpha_116usdt',
                levels: PartialDepthStreamLevelsEnum.LEVELS_5,
                interval: PartialDepthStreamIntervalEnum.INTERVAL_0ms,
            };
            const params = Object.assign({ ..._params });
            delete params?.levels;

            expect(() => websocketStreamApi.partialDepthStream(params)).toThrow(
                'Required parameter levels was null or undefined when calling partialDepthStream.'
            );
        });

        it('should throw RequiredError when interval is missing', () => {
            const configuration = new ConfigurationWebsocketStreams({});
            const websocketStreamClient = new WebsocketStreamsBase(configuration);
            const websocketStreamApi = new Api(websocketStreamClient);

            const _params: PartialDepthStreamRequest = {
                symbol: 'alpha_116usdt',
                levels: PartialDepthStreamLevelsEnum.LEVELS_5,
                interval: PartialDepthStreamIntervalEnum.INTERVAL_0ms,
            };
            const params = Object.assign({ ..._params });
            delete params?.interval;

            expect(() => websocketStreamApi.partialDepthStream(params)).toThrow(
                'Required parameter interval was null or undefined when calling partialDepthStream.'
            );
        });
    });

    describe('tickerStream()', () => {
        it('should execute tickerStream() successfully', async () => {
            const params: TickerStreamRequest = {
                symbol: 'alpha_116usdt',
                id: '1',
            };

            const mockResponse = JSONParse(
                JSONStringify({
                    e: '24hrTicker',
                    E: 1773109631569,
                    s: 'ALPHA_116USDT',
                    p: '0.00000000',
                    P: '0.00',
                    w: '8.40003127',
                    c: '8.40000000',
                    Q: '0.49293200',
                    o: '8.40000000',
                    h: '8.50000000',
                    l: '8.30000000',
                    v: '64750.63418500',
                    q: '543907.35169250',
                    O: 1773023220000,
                    C: 1773109631555,
                    F: 19847634,
                    L: 19911287,
                    n: 217505,
                })
            );

            mockSubscription(
                `ws/${replaceWebsocketStreamsPlaceholders('/<symbol>@ticker'.slice(1), params as unknown as Record<string, TickerStreamRequest>)}`,
                mockResponse
            );
        });

        it('should handle tickerStream() WebSocket stream data', () => {
            const configuration = new ConfigurationWebsocketStreams({});
            const websocketStreamClient = new WebsocketStreamsBase(configuration);
            const websocketStreamApi = new Api(websocketStreamClient);

            const params: TickerStreamRequest = {
                symbol: 'alpha_116usdt',
                id: '1',
            };

            const mockResponse = JSONParse(
                JSONStringify({
                    e: '24hrTicker',
                    E: 1773109631569,
                    s: 'ALPHA_116USDT',
                    p: '0.00000000',
                    P: '0.00',
                    w: '8.40003127',
                    c: '8.40000000',
                    Q: '0.49293200',
                    o: '8.40000000',
                    h: '8.50000000',
                    l: '8.30000000',
                    v: '64750.63418500',
                    q: '543907.35169250',
                    O: 1773023220000,
                    C: 1773109631555,
                    F: 19847634,
                    L: 19911287,
                    n: 217505,
                })
            );

            const stream = websocketStreamApi.tickerStream(params);
            const mockCallback = jest.fn(() => {});
            stream.on('message', mockCallback);

            websocketStreamClient['onMessage'](
                JSONStringify({
                    stream: replaceWebsocketStreamsPlaceholders(
                        '/<symbol>@ticker'.slice(1),
                        params as unknown as Record<string, TickerStreamRequest>
                    ),
                    data: mockResponse,
                }),
                websocketStreamClient.connectionPool[0]
            );

            expect(mockCallback).toHaveBeenCalledWith(mockResponse);
        });

        it('should throw RequiredError when symbol is missing', () => {
            const configuration = new ConfigurationWebsocketStreams({});
            const websocketStreamClient = new WebsocketStreamsBase(configuration);
            const websocketStreamApi = new Api(websocketStreamClient);

            const _params: TickerStreamRequest = {
                symbol: 'alpha_116usdt',
            };
            const params = Object.assign({ ..._params });
            delete params?.symbol;

            expect(() => websocketStreamApi.tickerStream(params)).toThrow(
                'Required parameter symbol was null or undefined when calling tickerStream.'
            );
        });
    });

    describe('tradeStream()', () => {
        it('should execute tradeStream() successfully', async () => {
            const params: TradeStreamRequest = {
                symbol: 'alpha_116usdt',
                id: '1',
            };

            const mockResponse = JSONParse(
                JSONStringify({
                    e: 'trade',
                    E: 1773110023891,
                    T: 1773110023877,
                    s: 'ALPHA_116USDT',
                    t: 19911650,
                    p: '8.40000000',
                    q: '0.97915700',
                    m: false,
                })
            );

            mockSubscription(
                `ws/${replaceWebsocketStreamsPlaceholders('/<symbol>@trade'.slice(1), params as unknown as Record<string, TradeStreamRequest>)}`,
                mockResponse
            );
        });

        it('should handle tradeStream() WebSocket stream data', () => {
            const configuration = new ConfigurationWebsocketStreams({});
            const websocketStreamClient = new WebsocketStreamsBase(configuration);
            const websocketStreamApi = new Api(websocketStreamClient);

            const params: TradeStreamRequest = {
                symbol: 'alpha_116usdt',
                id: '1',
            };

            const mockResponse = JSONParse(
                JSONStringify({
                    e: 'trade',
                    E: 1773110023891,
                    T: 1773110023877,
                    s: 'ALPHA_116USDT',
                    t: 19911650,
                    p: '8.40000000',
                    q: '0.97915700',
                    m: false,
                })
            );

            const stream = websocketStreamApi.tradeStream(params);
            const mockCallback = jest.fn(() => {});
            stream.on('message', mockCallback);

            websocketStreamClient['onMessage'](
                JSONStringify({
                    stream: replaceWebsocketStreamsPlaceholders(
                        '/<symbol>@trade'.slice(1),
                        params as unknown as Record<string, TradeStreamRequest>
                    ),
                    data: mockResponse,
                }),
                websocketStreamClient.connectionPool[0]
            );

            expect(mockCallback).toHaveBeenCalledWith(mockResponse);
        });

        it('should throw RequiredError when symbol is missing', () => {
            const configuration = new ConfigurationWebsocketStreams({});
            const websocketStreamClient = new WebsocketStreamsBase(configuration);
            const websocketStreamApi = new Api(websocketStreamClient);

            const _params: TradeStreamRequest = {
                symbol: 'alpha_116usdt',
            };
            const params = Object.assign({ ..._params });
            delete params?.symbol;

            expect(() => websocketStreamApi.tradeStream(params)).toThrow(
                'Required parameter symbol was null or undefined when calling tradeStream.'
            );
        });
    });
});
