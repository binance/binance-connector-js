/**
 * Prediction Trading REST API
 *
 * Place and manage prediction market orders, query positions, and transfer funds via the Prediction Trading REST API.
 *
 * The version of the OpenAPI document: 1.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { jest, expect, beforeEach, describe, it } from '@jest/globals';
import { JSONParse, JSONStringify } from 'json-with-bigint';
import { ConfigurationRestAPI, type RestApiResponse } from '@binance/common';

import { RedeemApi } from '../../../src/rest-api';
import { BatchRedeemRequest, GetRedeemStatusRequest } from '../../../src/rest-api';
import type { BatchRedeemResponse, GetRedeemStatusResponse } from '../../../src/rest-api/types';

describe('RedeemApi', () => {
    let client: RedeemApi;
    let config: ConfigurationRestAPI;
    let mockResponse: object = {};

    beforeEach(() => {
        config = new ConfigurationRestAPI({
            apiKey: 'test-api-key',
            apiSecret: 'test-api-secret',
            basePath: '',
        });
        client = new RedeemApi(config);
    });

    describe('batchRedeem()', () => {
        it('should execute batchRedeem() successfully with required parameters only', async () => {
            const params: BatchRedeemRequest = {
                walletAddress: '0x12e32db8817e292508c34111cbc4b23340df542c',
                walletId: '5b5c1ec3be4e4416a5872b21c1ca5d20',
                tokenIds: ['112233'],
            };

            mockResponse = JSONParse(
                JSONStringify({
                    batchId: 'batch_20260525_redeem_001',
                    results: [
                        {
                            requestId: 'req_001',
                            txHash: '0xabc123def456789abcdef123456789abcdef123456789abcdef123456789abcd',
                            status: 'PENDING',
                            error: 'error',
                        },
                    ],
                })
            );

            const spy = jest.spyOn(client, 'batchRedeem').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<BatchRedeemResponse>)
            );
            const response = await client.batchRedeem(params);
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should execute batchRedeem() successfully with optional parameters', async () => {
            const params: BatchRedeemRequest = {
                walletAddress: '0x12e32db8817e292508c34111cbc4b23340df542c',
                walletId: '5b5c1ec3be4e4416a5872b21c1ca5d20',
                tokenIds: ['112233'],
                chainId: '56',
            };

            mockResponse = JSONParse(
                JSONStringify({
                    batchId: 'batch_20260525_redeem_001',
                    results: [
                        {
                            requestId: 'req_001',
                            txHash: '0xabc123def456789abcdef123456789abcdef123456789abcdef123456789abcd',
                            status: 'PENDING',
                            error: 'error',
                        },
                    ],
                })
            );

            const spy = jest.spyOn(client, 'batchRedeem').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<BatchRedeemResponse>)
            );
            const response = await client.batchRedeem(params);
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should throw RequiredError when walletAddress is missing', async () => {
            const _params: BatchRedeemRequest = {
                walletAddress: '0x12e32db8817e292508c34111cbc4b23340df542c',
                walletId: '5b5c1ec3be4e4416a5872b21c1ca5d20',
                tokenIds: ['112233'],
            };
            const params = Object.assign({ ..._params });
            delete params?.walletAddress;

            await expect(client.batchRedeem(params)).rejects.toThrow(
                'Required parameter walletAddress was null or undefined when calling batchRedeem.'
            );
        });

        it('should throw RequiredError when walletId is missing', async () => {
            const _params: BatchRedeemRequest = {
                walletAddress: '0x12e32db8817e292508c34111cbc4b23340df542c',
                walletId: '5b5c1ec3be4e4416a5872b21c1ca5d20',
                tokenIds: ['112233'],
            };
            const params = Object.assign({ ..._params });
            delete params?.walletId;

            await expect(client.batchRedeem(params)).rejects.toThrow(
                'Required parameter walletId was null or undefined when calling batchRedeem.'
            );
        });

        it('should throw RequiredError when tokenIds is missing', async () => {
            const _params: BatchRedeemRequest = {
                walletAddress: '0x12e32db8817e292508c34111cbc4b23340df542c',
                walletId: '5b5c1ec3be4e4416a5872b21c1ca5d20',
                tokenIds: ['112233'],
            };
            const params = Object.assign({ ..._params });
            delete params?.tokenIds;

            await expect(client.batchRedeem(params)).rejects.toThrow(
                'Required parameter tokenIds was null or undefined when calling batchRedeem.'
            );
        });

        it('should throw an error when server is returning an error', async () => {
            const params: BatchRedeemRequest = {
                walletAddress: '0x12e32db8817e292508c34111cbc4b23340df542c',
                walletId: '5b5c1ec3be4e4416a5872b21c1ca5d20',
                tokenIds: ['112233'],
            };

            const errorResponse = {
                code: -1111,
                msg: 'Server Error',
            };

            const mockError = new Error('ResponseError') as Error & {
                response?: { status: number; data: unknown };
            };
            mockError.response = { status: 400, data: errorResponse };
            const spy = jest.spyOn(client, 'batchRedeem').mockRejectedValueOnce(mockError);
            await expect(client.batchRedeem(params)).rejects.toThrow('ResponseError');
            spy.mockRestore();
        });
    });

    describe('getRedeemStatus()', () => {
        it('should execute getRedeemStatus() successfully with required parameters only', async () => {
            const params: GetRedeemStatusRequest = {
                walletAddress: '0x12e32db8817e292508c34111cbc4b23340df542c',
                txHash: '0xabc123def456789abcdef123456789abcdef123456789abcdef123456789abcd',
            };

            mockResponse = JSONParse(
                JSONStringify({
                    txHash: '0xabc123def456789abcdef123456789abcdef123456789abcdef123456789abcd',
                    status: 'CONFIRMED',
                })
            );

            const spy = jest.spyOn(client, 'getRedeemStatus').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<GetRedeemStatusResponse>)
            );
            const response = await client.getRedeemStatus(params);
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should execute getRedeemStatus() successfully with optional parameters', async () => {
            const params: GetRedeemStatusRequest = {
                walletAddress: '0x12e32db8817e292508c34111cbc4b23340df542c',
                txHash: '0xabc123def456789abcdef123456789abcdef123456789abcdef123456789abcd',
                recvWindow: 5000,
            };

            mockResponse = JSONParse(
                JSONStringify({
                    txHash: '0xabc123def456789abcdef123456789abcdef123456789abcdef123456789abcd',
                    status: 'CONFIRMED',
                })
            );

            const spy = jest.spyOn(client, 'getRedeemStatus').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<GetRedeemStatusResponse>)
            );
            const response = await client.getRedeemStatus(params);
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should throw RequiredError when walletAddress is missing', async () => {
            const _params: GetRedeemStatusRequest = {
                walletAddress: '0x12e32db8817e292508c34111cbc4b23340df542c',
                txHash: '0xabc123def456789abcdef123456789abcdef123456789abcdef123456789abcd',
            };
            const params = Object.assign({ ..._params });
            delete params?.walletAddress;

            await expect(client.getRedeemStatus(params)).rejects.toThrow(
                'Required parameter walletAddress was null or undefined when calling getRedeemStatus.'
            );
        });

        it('should throw RequiredError when txHash is missing', async () => {
            const _params: GetRedeemStatusRequest = {
                walletAddress: '0x12e32db8817e292508c34111cbc4b23340df542c',
                txHash: '0xabc123def456789abcdef123456789abcdef123456789abcdef123456789abcd',
            };
            const params = Object.assign({ ..._params });
            delete params?.txHash;

            await expect(client.getRedeemStatus(params)).rejects.toThrow(
                'Required parameter txHash was null or undefined when calling getRedeemStatus.'
            );
        });

        it('should throw an error when server is returning an error', async () => {
            const params: GetRedeemStatusRequest = {
                walletAddress: '0x12e32db8817e292508c34111cbc4b23340df542c',
                txHash: '0xabc123def456789abcdef123456789abcdef123456789abcdef123456789abcd',
            };

            const errorResponse = {
                code: -1111,
                msg: 'Server Error',
            };

            const mockError = new Error('ResponseError') as Error & {
                response?: { status: number; data: unknown };
            };
            mockError.response = { status: 400, data: errorResponse };
            const spy = jest.spyOn(client, 'getRedeemStatus').mockRejectedValueOnce(mockError);
            await expect(client.getRedeemStatus(params)).rejects.toThrow('ResponseError');
            spy.mockRestore();
        });
    });
});
