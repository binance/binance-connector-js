/**
 * Prediction Trading REST API
 *
 * Place and manage prediction market orders, query positions, and transfer funds via the Prediction Trading REST API.
 *
 * The version of the OpenAPI document: 1.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { jest, expect, beforeEach, describe, it } from '@jest/globals';
import { JSONParse, JSONStringify } from 'json-with-bigint';
import { ConfigurationRestAPI, type RestApiResponse } from '@binance/common';

import {
    MarketDataApi,
    ListPredictionMarketsSortByEnum,
    ListPredictionMarketsOrderByEnum,
} from '../../../src/rest-api';
import {
    GetMarketDetailRequest,
    ListPredictionMarketsRequest,
    MarketSearchRequest,
    QueryLastTradePriceRequest,
    QueryOrderBookRequest,
} from '../../../src/rest-api';
import type {
    GetMarketDetailResponse,
    ListPredictionCategoriesResponse,
    ListPredictionMarketsResponse,
    MarketSearchResponse,
    QueryLastTradePriceResponse,
    QueryOrderBookResponse,
} from '../../../src/rest-api/types';

describe('MarketDataApi', () => {
    let client: MarketDataApi;
    let config: ConfigurationRestAPI;
    let mockResponse: object = {};

    beforeEach(() => {
        config = new ConfigurationRestAPI({
            apiKey: 'test-api-key',
            apiSecret: 'test-api-secret',
            basePath: '',
        });
        client = new MarketDataApi(config);
    });

    describe('getMarketDetail()', () => {
        it('should execute getMarketDetail() successfully with required parameters only', async () => {
            const params: GetMarketDetailRequest = {
                marketTopicId: 4229564,
            };

            mockResponse = JSONParse(
                JSONStringify({
                    marketTopicId: 4229564,
                    vendor: 'PREDICT_FUN',
                    chainId: '56',
                    slug: 'btc-price-1h-up-or-down',
                    title: 'BTC Price 1h Up or Down?',
                    question: 'Will BTC price go UP in the next 1 hour?',
                    description:
                        'Resolves YES if BTC spot price is higher than the starting price at resolution time.',
                    imageUrl: 'https://cdn.example.com/prediction/btc-1h.png',
                    topicType: 'FLAT',
                    chartType: 'CRYPTO_UP_DOWN',
                    symbol: 'BTCUSDT',
                    variantData: {
                        type: 'CRYPTO_UP_DOWN',
                        startPrice: '67890.12',
                        endPrice: 'endPrice',
                        priceFeedId: '0xpricefeedid123',
                        priceFeedProvider: 'BINANCE',
                        priceFeedSymbol: 'BTCUSDT',
                    },
                    participantCount: 3420,
                    collateral: 'USDT',
                    feeRateBps: 200,
                    slippageBps: 1200,
                    isYieldBearing: false,
                    tradeVolume: '158234.56',
                    liquidity: '45000.00',
                    publishedAt: 1748100000000,
                    startDate: 1748131200000,
                    endDate: 1748134800000,
                    status: 'REGISTERED',
                    timeline: [
                        {
                            marketTopicId: 4229560,
                            startDate: 1748127600000,
                            endDate: 1748131200000,
                        },
                    ],
                    markets: [
                        {
                            marketId: 5567895,
                            externalId: 'ext_001',
                            title: 'UP',
                            question: 'Will BTC go UP?',
                            description: 'Resolves YES if BTC price increases.',
                            conditionId: '0xabc123',
                            status: 'REGISTERED',
                            tradingStatus: 'OPEN',
                            tradeVolume: '90000.00',
                            liquidity: '25000.00',
                            decimalPrecision: 2,
                            outcomes: [
                                {
                                    name: 'YES',
                                    price: '0.52',
                                    chance: '0.52',
                                    index: 0,
                                    tokenId: '112233',
                                },
                            ],
                        },
                    ],
                })
            );

            const spy = jest.spyOn(client, 'getMarketDetail').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<GetMarketDetailResponse>)
            );
            const response = await client.getMarketDetail(params);
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should execute getMarketDetail() successfully with optional parameters', async () => {
            const params: GetMarketDetailRequest = {
                marketTopicId: 4229564,
            };

            mockResponse = JSONParse(
                JSONStringify({
                    marketTopicId: 4229564,
                    vendor: 'PREDICT_FUN',
                    chainId: '56',
                    slug: 'btc-price-1h-up-or-down',
                    title: 'BTC Price 1h Up or Down?',
                    question: 'Will BTC price go UP in the next 1 hour?',
                    description:
                        'Resolves YES if BTC spot price is higher than the starting price at resolution time.',
                    imageUrl: 'https://cdn.example.com/prediction/btc-1h.png',
                    topicType: 'FLAT',
                    chartType: 'CRYPTO_UP_DOWN',
                    symbol: 'BTCUSDT',
                    variantData: {
                        type: 'CRYPTO_UP_DOWN',
                        startPrice: '67890.12',
                        endPrice: 'endPrice',
                        priceFeedId: '0xpricefeedid123',
                        priceFeedProvider: 'BINANCE',
                        priceFeedSymbol: 'BTCUSDT',
                    },
                    participantCount: 3420,
                    collateral: 'USDT',
                    feeRateBps: 200,
                    slippageBps: 1200,
                    isYieldBearing: false,
                    tradeVolume: '158234.56',
                    liquidity: '45000.00',
                    publishedAt: 1748100000000,
                    startDate: 1748131200000,
                    endDate: 1748134800000,
                    status: 'REGISTERED',
                    timeline: [
                        {
                            marketTopicId: 4229560,
                            startDate: 1748127600000,
                            endDate: 1748131200000,
                        },
                    ],
                    markets: [
                        {
                            marketId: 5567895,
                            externalId: 'ext_001',
                            title: 'UP',
                            question: 'Will BTC go UP?',
                            description: 'Resolves YES if BTC price increases.',
                            conditionId: '0xabc123',
                            status: 'REGISTERED',
                            tradingStatus: 'OPEN',
                            tradeVolume: '90000.00',
                            liquidity: '25000.00',
                            decimalPrecision: 2,
                            outcomes: [
                                {
                                    name: 'YES',
                                    price: '0.52',
                                    chance: '0.52',
                                    index: 0,
                                    tokenId: '112233',
                                },
                            ],
                        },
                    ],
                })
            );

            const spy = jest.spyOn(client, 'getMarketDetail').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<GetMarketDetailResponse>)
            );
            const response = await client.getMarketDetail(params);
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should throw RequiredError when marketTopicId is missing', async () => {
            const _params: GetMarketDetailRequest = {
                marketTopicId: 4229564,
            };
            const params = Object.assign({ ..._params });
            delete params?.marketTopicId;

            await expect(client.getMarketDetail(params)).rejects.toThrow(
                'Required parameter marketTopicId was null or undefined when calling getMarketDetail.'
            );
        });

        it('should throw an error when server is returning an error', async () => {
            const params: GetMarketDetailRequest = {
                marketTopicId: 4229564,
            };

            const errorResponse = {
                code: -1111,
                msg: 'Server Error',
            };

            const mockError = new Error('ResponseError') as Error & {
                response?: { status: number; data: unknown };
            };
            mockError.response = { status: 400, data: errorResponse };
            const spy = jest.spyOn(client, 'getMarketDetail').mockRejectedValueOnce(mockError);
            await expect(client.getMarketDetail(params)).rejects.toThrow('ResponseError');
            spy.mockRestore();
        });
    });

    describe('listPredictionCategories()', () => {
        it('should execute listPredictionCategories() successfully with required parameters only', async () => {
            mockResponse = JSONParse(
                JSONStringify({
                    categories: [
                        {
                            id: 'crypto',
                            name: 'Crypto',
                            icon: 'crypto_icon',
                            order: 1,
                            subcategories: [
                                { id: 'up-down', name: 'Up/Down', icon: 'updown_icon', order: 1 },
                            ],
                        },
                    ],
                })
            );

            const spy = jest.spyOn(client, 'listPredictionCategories').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<ListPredictionCategoriesResponse>)
            );
            const response = await client.listPredictionCategories();
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should throw an error when server is returning an error', async () => {
            const errorResponse = {
                code: -1111,
                msg: 'Server Error',
            };

            const mockError = new Error('ResponseError') as Error & {
                response?: { status: number; data: unknown };
            };
            mockError.response = { status: 400, data: errorResponse };
            const spy = jest
                .spyOn(client, 'listPredictionCategories')
                .mockRejectedValueOnce(mockError);
            await expect(client.listPredictionCategories()).rejects.toThrow('ResponseError');
            spy.mockRestore();
        });
    });

    describe('listPredictionMarkets()', () => {
        it('should execute listPredictionMarkets() successfully with required parameters only', async () => {
            mockResponse = JSONParse(
                JSONStringify({
                    marketTopics: [
                        {
                            marketTopicId: 4229564,
                            vendor: 'PREDICT_FUN',
                            chainId: '56',
                            slug: 'btc-price-1h-up-or-down',
                            title: 'BTC Price 1h Up or Down?',
                            question: 'Will BTC price go UP in the next 1 hour?',
                            description:
                                'Resolves YES if BTC spot price is higher than the starting price at resolution time.',
                            imageUrl: 'https://cdn.example.com/prediction/btc-1h.png',
                            topicType: 'FLAT',
                            chartType: 'CRYPTO_UP_DOWN',
                            symbol: 'BTCUSDT',
                            participantCount: 3420,
                            collateral: 'USDT',
                            feeRateBps: 200,
                            slippageBps: 1200,
                            isYieldBearing: false,
                            tradeVolume: '158234.56',
                            liquidity: '45000.00',
                            publishedAt: 1748100000000,
                            startDate: 1748131200000,
                            endDate: 1748134800000,
                            status: 'REGISTERED',
                            markets: [{}],
                        },
                    ],
                    total: 128,
                    offset: 0,
                    limit: 20,
                    hasMore: true,
                })
            );

            const spy = jest.spyOn(client, 'listPredictionMarkets').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<ListPredictionMarketsResponse>)
            );
            const response = await client.listPredictionMarkets();
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should execute listPredictionMarkets() successfully with optional parameters', async () => {
            const params: ListPredictionMarketsRequest = {
                l1Category: 'crypto',
                l2Category: 'up-down',
                sortBy: ListPredictionMarketsSortByEnum.RECOMMENDED,
                orderBy: ListPredictionMarketsOrderByEnum.ASC,
                offset: 0,
                limit: 20,
            };

            mockResponse = JSONParse(
                JSONStringify({
                    marketTopics: [
                        {
                            marketTopicId: 4229564,
                            vendor: 'PREDICT_FUN',
                            chainId: '56',
                            slug: 'btc-price-1h-up-or-down',
                            title: 'BTC Price 1h Up or Down?',
                            question: 'Will BTC price go UP in the next 1 hour?',
                            description:
                                'Resolves YES if BTC spot price is higher than the starting price at resolution time.',
                            imageUrl: 'https://cdn.example.com/prediction/btc-1h.png',
                            topicType: 'FLAT',
                            chartType: 'CRYPTO_UP_DOWN',
                            symbol: 'BTCUSDT',
                            participantCount: 3420,
                            collateral: 'USDT',
                            feeRateBps: 200,
                            slippageBps: 1200,
                            isYieldBearing: false,
                            tradeVolume: '158234.56',
                            liquidity: '45000.00',
                            publishedAt: 1748100000000,
                            startDate: 1748131200000,
                            endDate: 1748134800000,
                            status: 'REGISTERED',
                            markets: [{}],
                        },
                    ],
                    total: 128,
                    offset: 0,
                    limit: 20,
                    hasMore: true,
                })
            );

            const spy = jest.spyOn(client, 'listPredictionMarkets').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<ListPredictionMarketsResponse>)
            );
            const response = await client.listPredictionMarkets(params);
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should throw an error when server is returning an error', async () => {
            const errorResponse = {
                code: -1111,
                msg: 'Server Error',
            };

            const mockError = new Error('ResponseError') as Error & {
                response?: { status: number; data: unknown };
            };
            mockError.response = { status: 400, data: errorResponse };
            const spy = jest
                .spyOn(client, 'listPredictionMarkets')
                .mockRejectedValueOnce(mockError);
            await expect(client.listPredictionMarkets()).rejects.toThrow('ResponseError');
            spy.mockRestore();
        });
    });

    describe('marketSearch()', () => {
        it('should execute marketSearch() successfully with required parameters only', async () => {
            const params: MarketSearchRequest = {
                query: 'BTC price',
            };

            mockResponse = JSONParse(
                JSONStringify([
                    {
                        marketTopicId: 4229564,
                        vendor: 'PREDICT_FUN',
                        chainId: '56',
                        slug: 'btc-price-1h-up-or-down',
                        title: 'BTC Price 1h Up or Down?',
                        question: 'Will BTC price go UP in the next 1 hour?',
                        description:
                            'Resolves YES if BTC spot price is higher than the starting price.',
                        topicType: 'FLAT',
                        chartType: 'CRYPTO_UP_DOWN',
                        symbol: 'BTCUSDT',
                        participantCount: 3420,
                        collateral: 'USDT',
                        tradeVolume: '158234.56',
                        liquidity: '45000.00',
                        startDate: 1748131200000,
                        endDate: 1748134800000,
                        status: 'REGISTERED',
                        markets: [{}],
                    },
                ])
            );

            const spy = jest.spyOn(client, 'marketSearch').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<MarketSearchResponse>)
            );
            const response = await client.marketSearch(params);
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should execute marketSearch() successfully with optional parameters', async () => {
            const params: MarketSearchRequest = {
                query: 'BTC price',
                topK: 20,
            };

            mockResponse = JSONParse(
                JSONStringify([
                    {
                        marketTopicId: 4229564,
                        vendor: 'PREDICT_FUN',
                        chainId: '56',
                        slug: 'btc-price-1h-up-or-down',
                        title: 'BTC Price 1h Up or Down?',
                        question: 'Will BTC price go UP in the next 1 hour?',
                        description:
                            'Resolves YES if BTC spot price is higher than the starting price.',
                        topicType: 'FLAT',
                        chartType: 'CRYPTO_UP_DOWN',
                        symbol: 'BTCUSDT',
                        participantCount: 3420,
                        collateral: 'USDT',
                        tradeVolume: '158234.56',
                        liquidity: '45000.00',
                        startDate: 1748131200000,
                        endDate: 1748134800000,
                        status: 'REGISTERED',
                        markets: [{}],
                    },
                ])
            );

            const spy = jest.spyOn(client, 'marketSearch').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<MarketSearchResponse>)
            );
            const response = await client.marketSearch(params);
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should throw RequiredError when query is missing', async () => {
            const _params: MarketSearchRequest = {
                query: 'BTC price',
            };
            const params = Object.assign({ ..._params });
            delete params?.query;

            await expect(client.marketSearch(params)).rejects.toThrow(
                'Required parameter query was null or undefined when calling marketSearch.'
            );
        });

        it('should throw an error when server is returning an error', async () => {
            const params: MarketSearchRequest = {
                query: 'BTC price',
            };

            const errorResponse = {
                code: -1111,
                msg: 'Server Error',
            };

            const mockError = new Error('ResponseError') as Error & {
                response?: { status: number; data: unknown };
            };
            mockError.response = { status: 400, data: errorResponse };
            const spy = jest.spyOn(client, 'marketSearch').mockRejectedValueOnce(mockError);
            await expect(client.marketSearch(params)).rejects.toThrow('ResponseError');
            spy.mockRestore();
        });
    });

    describe('queryLastTradePrice()', () => {
        it('should execute queryLastTradePrice() successfully with required parameters only', async () => {
            const params: QueryLastTradePriceRequest = {
                marketId: 5567895,
            };

            mockResponse = JSONParse(JSONStringify({ marketId: 5567895, lastTradePrice: '0.52' }));

            const spy = jest.spyOn(client, 'queryLastTradePrice').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<QueryLastTradePriceResponse>)
            );
            const response = await client.queryLastTradePrice(params);
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should execute queryLastTradePrice() successfully with optional parameters', async () => {
            const params: QueryLastTradePriceRequest = {
                marketId: 5567895,
            };

            mockResponse = JSONParse(JSONStringify({ marketId: 5567895, lastTradePrice: '0.52' }));

            const spy = jest.spyOn(client, 'queryLastTradePrice').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<QueryLastTradePriceResponse>)
            );
            const response = await client.queryLastTradePrice(params);
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should throw RequiredError when marketId is missing', async () => {
            const _params: QueryLastTradePriceRequest = {
                marketId: 5567895,
            };
            const params = Object.assign({ ..._params });
            delete params?.marketId;

            await expect(client.queryLastTradePrice(params)).rejects.toThrow(
                'Required parameter marketId was null or undefined when calling queryLastTradePrice.'
            );
        });

        it('should throw an error when server is returning an error', async () => {
            const params: QueryLastTradePriceRequest = {
                marketId: 5567895,
            };

            const errorResponse = {
                code: -1111,
                msg: 'Server Error',
            };

            const mockError = new Error('ResponseError') as Error & {
                response?: { status: number; data: unknown };
            };
            mockError.response = { status: 400, data: errorResponse };
            const spy = jest.spyOn(client, 'queryLastTradePrice').mockRejectedValueOnce(mockError);
            await expect(client.queryLastTradePrice(params)).rejects.toThrow('ResponseError');
            spy.mockRestore();
        });
    });

    describe('queryOrderBook()', () => {
        it('should execute queryOrderBook() successfully with required parameters only', async () => {
            const params: QueryOrderBookRequest = {
                vendor: 'predict_fun',
                marketId: 5567895,
                tokenId: '112233',
            };

            mockResponse = JSONParse(
                JSONStringify({
                    outcome: 'YES',
                    tokenId: '112233',
                    timestamp: 1748131800000,
                    bids: [{ price: '0.51', size: '5000.00' }],
                    asks: [{ price: '0.52', size: '3000.00' }],
                })
            );

            const spy = jest.spyOn(client, 'queryOrderBook').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<QueryOrderBookResponse>)
            );
            const response = await client.queryOrderBook(params);
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should execute queryOrderBook() successfully with optional parameters', async () => {
            const params: QueryOrderBookRequest = {
                vendor: 'predict_fun',
                marketId: 5567895,
                tokenId: '112233',
            };

            mockResponse = JSONParse(
                JSONStringify({
                    outcome: 'YES',
                    tokenId: '112233',
                    timestamp: 1748131800000,
                    bids: [{ price: '0.51', size: '5000.00' }],
                    asks: [{ price: '0.52', size: '3000.00' }],
                })
            );

            const spy = jest.spyOn(client, 'queryOrderBook').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<QueryOrderBookResponse>)
            );
            const response = await client.queryOrderBook(params);
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should throw RequiredError when vendor is missing', async () => {
            const _params: QueryOrderBookRequest = {
                vendor: 'predict_fun',
                marketId: 5567895,
                tokenId: '112233',
            };
            const params = Object.assign({ ..._params });
            delete params?.vendor;

            await expect(client.queryOrderBook(params)).rejects.toThrow(
                'Required parameter vendor was null or undefined when calling queryOrderBook.'
            );
        });

        it('should throw RequiredError when marketId is missing', async () => {
            const _params: QueryOrderBookRequest = {
                vendor: 'predict_fun',
                marketId: 5567895,
                tokenId: '112233',
            };
            const params = Object.assign({ ..._params });
            delete params?.marketId;

            await expect(client.queryOrderBook(params)).rejects.toThrow(
                'Required parameter marketId was null or undefined when calling queryOrderBook.'
            );
        });

        it('should throw RequiredError when tokenId is missing', async () => {
            const _params: QueryOrderBookRequest = {
                vendor: 'predict_fun',
                marketId: 5567895,
                tokenId: '112233',
            };
            const params = Object.assign({ ..._params });
            delete params?.tokenId;

            await expect(client.queryOrderBook(params)).rejects.toThrow(
                'Required parameter tokenId was null or undefined when calling queryOrderBook.'
            );
        });

        it('should throw an error when server is returning an error', async () => {
            const params: QueryOrderBookRequest = {
                vendor: 'predict_fun',
                marketId: 5567895,
                tokenId: '112233',
            };

            const errorResponse = {
                code: -1111,
                msg: 'Server Error',
            };

            const mockError = new Error('ResponseError') as Error & {
                response?: { status: number; data: unknown };
            };
            mockError.response = { status: 400, data: errorResponse };
            const spy = jest.spyOn(client, 'queryOrderBook').mockRejectedValueOnce(mockError);
            await expect(client.queryOrderBook(params)).rejects.toThrow('ResponseError');
            spy.mockRestore();
        });
    });
});
