/**
 * Prediction Trading REST API
 *
 * Place and manage prediction market orders, query positions, and transfer funds via the Prediction Trading REST API.
 *
 * The version of the OpenAPI document: 1.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { jest, expect, beforeEach, describe, it } from '@jest/globals';
import { JSONParse, JSONStringify } from 'json-with-bigint';
import { ConfigurationRestAPI, type RestApiResponse } from '@binance/common';

import { WalletApi } from '../../../src/rest-api';
import {
    GetPortfolioRequest,
    GetQuotaStatusRequest,
    ListPredictionWalletsRequest,
    QueryPaymentOptionBalancesRequest,
} from '../../../src/rest-api';
import type {
    GetPortfolioResponse,
    GetQuotaStatusResponse,
    ListPredictionWalletsResponse,
    QueryPaymentOptionBalancesResponse,
} from '../../../src/rest-api/types';

describe('WalletApi', () => {
    let client: WalletApi;
    let config: ConfigurationRestAPI;
    let mockResponse: object = {};

    beforeEach(() => {
        config = new ConfigurationRestAPI({
            apiKey: 'test-api-key',
            apiSecret: 'test-api-secret',
            basePath: '',
        });
        client = new WalletApi(config);
    });

    describe('getPortfolio()', () => {
        it('should execute getPortfolio() successfully with required parameters only', async () => {
            const params: GetPortfolioRequest = {
                walletAddress: '0x12e32db8817e292508c34111cbc4b23340df542c',
            };

            mockResponse = JSONParse(
                JSONStringify({
                    chainId: '56',
                    walletAddress: '0x12e32db8817e292508c34111cbc4b23340df542c',
                    activePositionsCount: 3,
                    totalRealizedPnl: '120.50',
                    totalUnrealizedPnl: '15.30',
                    totalPnl: '135.80',
                    totalCostBasis: '450.00',
                    totalCurrentValue: '465.30',
                    positions: [
                        {
                            id: 10001,
                            walletAddress: '0x12e32db8817e292508c34111cbc4b23340df542c',
                            marketTopicId: 4229564,
                            marketId: 5567895,
                            tokenId: '112233',
                            vendor: 'PREDICT_FUN',
                            currentShares: '1923.07',
                            avgPrice: '0.52',
                            currentPrice: '0.55',
                            realizedPnl: '0.00',
                            unrealizedPnl: '0.06',
                            totalPnl: '0.06',
                            pnlPercentage: '6.00',
                            isResolved: false,
                        },
                    ],
                })
            );

            const spy = jest.spyOn(client, 'getPortfolio').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<GetPortfolioResponse>)
            );
            const response = await client.getPortfolio(params);
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should execute getPortfolio() successfully with optional parameters', async () => {
            const params: GetPortfolioRequest = {
                walletAddress: '0x12e32db8817e292508c34111cbc4b23340df542c',
                tokenId: '112233',
                marketId: 5567895,
                marketTopicId: 4229564,
                activeOnly: false,
                recvWindow: 5000,
            };

            mockResponse = JSONParse(
                JSONStringify({
                    chainId: '56',
                    walletAddress: '0x12e32db8817e292508c34111cbc4b23340df542c',
                    activePositionsCount: 3,
                    totalRealizedPnl: '120.50',
                    totalUnrealizedPnl: '15.30',
                    totalPnl: '135.80',
                    totalCostBasis: '450.00',
                    totalCurrentValue: '465.30',
                    positions: [
                        {
                            id: 10001,
                            walletAddress: '0x12e32db8817e292508c34111cbc4b23340df542c',
                            marketTopicId: 4229564,
                            marketId: 5567895,
                            tokenId: '112233',
                            vendor: 'PREDICT_FUN',
                            currentShares: '1923.07',
                            avgPrice: '0.52',
                            currentPrice: '0.55',
                            realizedPnl: '0.00',
                            unrealizedPnl: '0.06',
                            totalPnl: '0.06',
                            pnlPercentage: '6.00',
                            isResolved: false,
                        },
                    ],
                })
            );

            const spy = jest.spyOn(client, 'getPortfolio').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<GetPortfolioResponse>)
            );
            const response = await client.getPortfolio(params);
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should throw RequiredError when walletAddress is missing', async () => {
            const _params: GetPortfolioRequest = {
                walletAddress: '0x12e32db8817e292508c34111cbc4b23340df542c',
            };
            const params = Object.assign({ ..._params });
            delete params?.walletAddress;

            await expect(client.getPortfolio(params)).rejects.toThrow(
                'Required parameter walletAddress was null or undefined when calling getPortfolio.'
            );
        });

        it('should throw an error when server is returning an error', async () => {
            const params: GetPortfolioRequest = {
                walletAddress: '0x12e32db8817e292508c34111cbc4b23340df542c',
            };

            const errorResponse = {
                code: -1111,
                msg: 'Server Error',
            };

            const mockError = new Error('ResponseError') as Error & {
                response?: { status: number; data: unknown };
            };
            mockError.response = { status: 400, data: errorResponse };
            const spy = jest.spyOn(client, 'getPortfolio').mockRejectedValueOnce(mockError);
            await expect(client.getPortfolio(params)).rejects.toThrow('ResponseError');
            spy.mockRestore();
        });
    });

    describe('getQuotaStatus()', () => {
        it('should execute getQuotaStatus() successfully with required parameters only', async () => {
            mockResponse = JSONParse(
                JSONStringify({ dailyLimit: '10000.00', remainingDailyLimit: '8500.00' })
            );

            const spy = jest.spyOn(client, 'getQuotaStatus').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<GetQuotaStatusResponse>)
            );
            const response = await client.getQuotaStatus();
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should execute getQuotaStatus() successfully with optional parameters', async () => {
            const params: GetQuotaStatusRequest = {
                recvWindow: 5000,
            };

            mockResponse = JSONParse(
                JSONStringify({ dailyLimit: '10000.00', remainingDailyLimit: '8500.00' })
            );

            const spy = jest.spyOn(client, 'getQuotaStatus').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<GetQuotaStatusResponse>)
            );
            const response = await client.getQuotaStatus(params);
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should throw an error when server is returning an error', async () => {
            const errorResponse = {
                code: -1111,
                msg: 'Server Error',
            };

            const mockError = new Error('ResponseError') as Error & {
                response?: { status: number; data: unknown };
            };
            mockError.response = { status: 400, data: errorResponse };
            const spy = jest.spyOn(client, 'getQuotaStatus').mockRejectedValueOnce(mockError);
            await expect(client.getQuotaStatus()).rejects.toThrow('ResponseError');
            spy.mockRestore();
        });
    });

    describe('listPredictionWallets()', () => {
        it('should execute listPredictionWallets() successfully with required parameters only', async () => {
            mockResponse = JSONParse(
                JSONStringify({
                    wallets: [
                        {
                            walletAddress: '0x12e32db8817e292508c34111cbc4b23340df542c',
                            walletId: '5b5c1ec3be4e4416a5872b21c1ca5d20',
                            registeredTime: 1748000000000,
                        },
                    ],
                })
            );

            const spy = jest.spyOn(client, 'listPredictionWallets').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<ListPredictionWalletsResponse>)
            );
            const response = await client.listPredictionWallets();
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should execute listPredictionWallets() successfully with optional parameters', async () => {
            const params: ListPredictionWalletsRequest = {
                recvWindow: 5000,
            };

            mockResponse = JSONParse(
                JSONStringify({
                    wallets: [
                        {
                            walletAddress: '0x12e32db8817e292508c34111cbc4b23340df542c',
                            walletId: '5b5c1ec3be4e4416a5872b21c1ca5d20',
                            registeredTime: 1748000000000,
                        },
                    ],
                })
            );

            const spy = jest.spyOn(client, 'listPredictionWallets').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<ListPredictionWalletsResponse>)
            );
            const response = await client.listPredictionWallets(params);
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should throw an error when server is returning an error', async () => {
            const errorResponse = {
                code: -1111,
                msg: 'Server Error',
            };

            const mockError = new Error('ResponseError') as Error & {
                response?: { status: number; data: unknown };
            };
            mockError.response = { status: 400, data: errorResponse };
            const spy = jest
                .spyOn(client, 'listPredictionWallets')
                .mockRejectedValueOnce(mockError);
            await expect(client.listPredictionWallets()).rejects.toThrow('ResponseError');
            spy.mockRestore();
        });
    });

    describe('queryPaymentOptionBalances()', () => {
        it('should execute queryPaymentOptionBalances() successfully with required parameters only', async () => {
            mockResponse = JSONParse(
                JSONStringify({
                    items: [
                        { accountType: 'SPOT', availableBalanceDisplay: '1000.00', enabled: true },
                    ],
                })
            );

            const spy = jest.spyOn(client, 'queryPaymentOptionBalances').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<QueryPaymentOptionBalancesResponse>)
            );
            const response = await client.queryPaymentOptionBalances();
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should execute queryPaymentOptionBalances() successfully with optional parameters', async () => {
            const params: QueryPaymentOptionBalancesRequest = {
                recvWindow: 5000,
            };

            mockResponse = JSONParse(
                JSONStringify({
                    items: [
                        { accountType: 'SPOT', availableBalanceDisplay: '1000.00', enabled: true },
                    ],
                })
            );

            const spy = jest.spyOn(client, 'queryPaymentOptionBalances').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<QueryPaymentOptionBalancesResponse>)
            );
            const response = await client.queryPaymentOptionBalances(params);
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should throw an error when server is returning an error', async () => {
            const errorResponse = {
                code: -1111,
                msg: 'Server Error',
            };

            const mockError = new Error('ResponseError') as Error & {
                response?: { status: number; data: unknown };
            };
            mockError.response = { status: 400, data: errorResponse };
            const spy = jest
                .spyOn(client, 'queryPaymentOptionBalances')
                .mockRejectedValueOnce(mockError);
            await expect(client.queryPaymentOptionBalances()).rejects.toThrow('ResponseError');
            spy.mockRestore();
        });
    });
});
